import Foundation

/// {{className}} holds optional parameters for the {{operationId}} operation.
public struct {{className}}: Sendable {
{{#params}}
{{#description}}
    /// {{{.}}}
{{/description}}
{{#required}}
    public var {{name}}: {{{dataType}}}
{{/required}}
{{^required}}
    public var {{name}}: {{{dataType}}}?
{{/required}}
{{/params}}
{{#hasAuthField}}
    /// Per-operation authenticator. When set, it overrides the client's
    /// configured credentials for this call only; when nil, the configured
    /// credentials are used.
    public var auth: {{authFieldType}}?
{{/hasAuthField}}

    public init({{#params}}{{name}}: {{#required}}{{{dataType}}}{{/required}}{{^required}}{{{dataType}}}? = nil{{/required}}, {{/params}}{{#hasAuthField}}auth: {{authFieldType}}? = nil{{/hasAuthField}}) {
{{#params}}
        self.{{name}} = {{name}}
{{/params}}
{{#hasAuthField}}
        self.auth = auth
{{/hasAuthField}}
    }
}
