{{>partial_header}}
from typing import Any, Optional

from {{packageName}}.exceptions.client_exception import ClientException


class ForbiddenException(ClientException):
    """Exception for HTTP 403 Forbidden."""

    def __init__(
        self,
        reason: Optional[str] = None,
        http_resp: Optional[Any] = None,
        *,
        body: Optional[str] = None,
        data: Optional[Any] = None,
        error_body: Optional[Any] = None,
    ) -> None:
        super().__init__(status=403, reason=reason, http_resp=http_resp, body=body, data=data, error_body=error_body)
