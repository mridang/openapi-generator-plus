{{>partial_header}}
from typing import Any, Optional

from {{packageName}}.exceptions.server_exception import ServerException


class InternalServerErrorException(ServerException):
    """Exception for HTTP 500 Internal Server Error."""

    def __init__(
        self,
        message: Optional[str] = None,
        http_resp: Optional[Any] = None,
        *,
        response_body: Optional[str] = None,
        response_headers: Optional[Any] = None,
        error_body: Optional[Any] = None,
    ) -> None:
        super().__init__(code=500, message=message, http_resp=http_resp, response_body=response_body, response_headers=response_headers, error_body=error_body)
