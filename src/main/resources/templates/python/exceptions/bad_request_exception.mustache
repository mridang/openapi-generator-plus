{{>partial_header}}
from typing import Any, Optional

from {{packageName}}.exceptions.client_exception import ClientException


class BadRequestException(ClientException):
    """Exception for HTTP 400 Bad Request."""

    def __init__(
        self,
        reason: Optional[str] = None,
        http_resp: Optional[Any] = None,
        *,
        body: Optional[str] = None,
        data: Optional[Any] = None,
    ) -> None:
        super().__init__(status=400, reason=reason, http_resp=http_resp, body=body, data=data)
