{{>api_info}}

from {{packageName}}.errors.network_exception import NetworkException


class NetworkTimeoutException(NetworkException):
    """Exception for a request that timed out.

    The status code is always ``0``; the underlying library error is kept
    as ``__cause__``.
    """

    pass
