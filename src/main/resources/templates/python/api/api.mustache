from typing import Any, Dict, List, Optional, Union
from urllib.parse import quote

{{#imports}}
{{import}}
{{/imports}}

from ..api_client import ApiClient
from ..default_api_client import DefaultApiClient
from ..configuration import Configuration
from .base_api import BaseApi
from ..object_serializer import ObjectSerializer
{{#hasAnyAuthMethods}}
from ..auth.authenticator import Authenticator
{{/hasAnyAuthMethods}}


{{#operations}}
class {{classname}}(BaseApi):
    """{{classname}} provides methods for the {{baseName}} API group."""

    def __init__(
        self,
        api_client: Optional[ApiClient] = None,
        config: Optional[Configuration] = None,
    ):
        super().__init__(api_client, config)
{{#operation}}

    def {{operationId}}(
        self,
{{#hasAuthMethods}}
        auth: Authenticator,
{{/hasAuthMethods}}
        {{#allParams}}
        {{paramName}}: {{^required}}Optional[{{/required}}{{{dataType}}}{{^required}}] = None{{/required}},
        {{/allParams}}
    ) -> {{#returnType}}{{{returnType}}}{{/returnType}}{{^returnType}}None{{/returnType}}:
        """{{summary}}
{{#notes}}
        {{.}}
{{/notes}}
{{#hasAuthMethods}}
        :param auth: authenticator for this operation
{{/hasAuthMethods}}
{{#allParams}}
        :param {{paramName}}: {{description}}{{#required}} (required){{/required}}{{^required}} (optional{{^isContainer}}{{#defaultValue}}, default to {{.}}{{/defaultValue}}{{/isContainer}}){{/required}}
{{/allParams}}
{{#returnType}}
        :return: {{{returnType}}}
{{/returnType}}
{{#isDeprecated}}
        .. deprecated::
{{/isDeprecated}}
        """
{{#allParams}}
{{#required}}
        if {{paramName}} is None:
            raise ValueError("Missing the required parameter '{{paramName}}' when calling {{operationId}}")
{{/required}}
{{/allParams}}
        path = '{{{path}}}'{{#pathParams}}
        path = path.replace('{' + '{{baseName}}' + '}', quote(ObjectSerializer.to_path_value({{paramName}}), safe='')){{/pathParams}}
        query_params: Dict[str, Any] = {}
{{#queryParams}}
        if {{paramName}} is not None:
            query_params['{{baseName}}'] = ObjectSerializer.to_query_value({{paramName}}{{#collectionFormat}}, '{{collectionFormat}}'{{/collectionFormat}})
{{/queryParams}}
        header_params: Dict[str, str] = {}
{{#headerParams}}
        if {{paramName}} is not None:
            header_params['{{baseName}}'] = ObjectSerializer.to_header_value({{paramName}})
{{/headerParams}}
{{#hasFormParams}}
        body: Dict[str, Any] = {}
{{#formParams}}
        if {{paramName}} is not None:
            body['{{baseName}}'] = ObjectSerializer.to_form_value({{paramName}})
{{/formParams}}
{{/hasFormParams}}
{{^hasFormParams}}
        body = {{#bodyParam}}{{paramName}}{{/bodyParam}}{{^bodyParam}}None{{/bodyParam}}
{{/hasFormParams}}

        return self.invoke_api(
            '{{httpMethod}}',
            path,
            query_params,
            header_params,
            body,
            [{{#produces}}'{{{mediaType}}}'{{^-last}}, {{/-last}}{{/produces}}],
            {{#consumes.0}}'{{{mediaType}}}'{{/consumes.0}}{{^consumes}}'application/json'{{/consumes}},
            {{#returnType}}'{{{returnType}}}'{{/returnType}}{{^returnType}}None{{/returnType}},
            {{#hasAuthMethods}}auth{{/hasAuthMethods}}{{^hasAuthMethods}}None{{/hasAuthMethods}},
        )
{{/operation}}
{{/operations}}
