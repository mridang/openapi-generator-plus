module {{moduleName}}
  module Api
    module Options
      # Required parameters have non-optional keyword arguments;
      # optional parameters default to nil.  The attr_reader types
      # are nullable only for optional parameters.  Instances are
      # frozen after construction, so only readers are exposed.
      class {{className}}
{{#params}}
{{#required}}
        attr_reader {{paramName}}: {{rbsType}}
{{/required}}
{{^required}}
        attr_reader {{paramName}}: {{rbsType}}?
{{/required}}
{{/params}}
{{#hasAuthField}}
        attr_reader auth: ::{{moduleName}}::Auth::Authenticator?
{{/hasAuthField}}
        def initialize: ({{initializeSignature}}) -> void
      end
    end
  end
end
