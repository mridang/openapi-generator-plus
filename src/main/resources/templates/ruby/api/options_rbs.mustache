module {{moduleName}}
  module Api
    module Options
      # Required parameters have non-optional keyword arguments;
      # optional parameters default to nil.  The attr_accessor types
      # are nullable only for optional parameters.
      class {{className}}
{{#params}}
{{#required}}
        attr_accessor {{paramName}}: {{rbsType}}
{{/required}}
{{^required}}
        attr_accessor {{paramName}}: {{rbsType}}?
{{/required}}
{{/params}}
{{#hasAuthField}}
        attr_accessor auth: ::{{moduleName}}::Auth::Authenticator?
{{/hasAuthField}}
        def initialize: ({{initializeSignature}}) -> void
      end
    end
  end
end
