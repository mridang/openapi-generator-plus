{{>api_info}}
{{#models}}
{{#model}}
import 'dart:convert';
import 'dart:typed_data';

import '../object_serializer.dart';

{{#hasDartImports}}
{{#dartImports}}
import '{{filename}}.dart';
{{/dartImports}}

{{/hasDartImports}}
{{#isEnum}}
{{#isInteger}}
/// {{classname}} is an enumeration of allowed integer values.
enum {{classname}} {
{{#allowableValues}}
{{#enumVars}}
  /// Represents the value {{{value}}}.
  {{name}}({{{value}}}),
{{/enumVars}}
{{/allowableValues}}
  ;

  final int value;
  const {{classname}}(this.value);

  /// Returns the enum value matching the given [value], or throws.
  static {{classname}} fromJson(int value) {
    return {{classname}}.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown {{classname}} value: $value'),
    );
  }

  int toJson() => value;

  @override
  String toString() => value.toString();
}
{{/isInteger}}
{{^isInteger}}
{{#isLong}}
/// {{classname}} is an enumeration of allowed integer values.
enum {{classname}} {
{{#allowableValues}}
{{#enumVars}}
  /// Represents the value {{{value}}}.
  {{name}}({{{value}}}),
{{/enumVars}}
{{/allowableValues}}
  ;

  final int value;
  const {{classname}}(this.value);

  /// Returns the enum value matching the given [value], or throws.
  static {{classname}} fromJson(int value) {
    return {{classname}}.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown {{classname}} value: $value'),
    );
  }

  int toJson() => value;

  @override
  String toString() => value.toString();
}
{{/isLong}}
{{^isLong}}
/// {{classname}} is an enumeration of allowed values.
enum {{classname}} {
{{#allowableValues}}
{{#enumVars}}
  /// Represents the value {{{value}}}.
  {{name}}({{{value}}}),
{{/enumVars}}
{{/allowableValues}}
  ;

  final String value;
  const {{classname}}(this.value);

  /// Returns the enum value matching the given [value], or throws.
  static {{classname}} fromJson(String value) {
    return {{classname}}.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown {{classname}} value: $value'),
    );
  }

  String toJson() => value;

  @override
  String toString() => value;
}
{{/isLong}}
{{/isInteger}}
{{/isEnum}}
{{^isEnum}}
{{^oneOf}}
{{^anyOf}}
{{^isPrimitiveOneOf}}
{{^isPrimitiveAnyOf}}
{{#description}}
/// {{classname}} {{{.}}}
{{/description}}
{{^description}}
/// {{classname}} is a model class generated from the OpenAPI schema.
{{/description}}
{{#isDeprecated}}
@Deprecated('This schema is deprecated.')
{{/isDeprecated}}
{{#vars}}
{{#isEnum}}
{{#isInteger}}
/// Typed integer enum for {{classname}}.{{name}}.
enum {{classname}}{{enumName}} {
{{#allowableValues}}
{{#enumVars}}
  /// Represents the value {{{value}}}.
  {{nameLowercase}}({{{value}}}),
{{/enumVars}}
{{/allowableValues}}
  ;

  final int value;
  const {{classname}}{{enumName}}(this.value);

  /// Returns the enum value matching the given [value], or throws.
  static {{classname}}{{enumName}} fromJson(int value) {
    return {{classname}}{{enumName}}.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown {{classname}}{{enumName}} value: $value'),
    );
  }

  int toJson() => value;

  @override
  String toString() => value.toString();
}

{{/isInteger}}
{{^isInteger}}
{{#isLong}}
/// Typed integer enum for {{classname}}.{{name}}.
enum {{classname}}{{enumName}} {
{{#allowableValues}}
{{#enumVars}}
  /// Represents the value {{{value}}}.
  {{nameLowercase}}({{{value}}}),
{{/enumVars}}
{{/allowableValues}}
  ;

  final int value;
  const {{classname}}{{enumName}}(this.value);

  /// Returns the enum value matching the given [value], or throws.
  static {{classname}}{{enumName}} fromJson(int value) {
    return {{classname}}{{enumName}}.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown {{classname}}{{enumName}} value: $value'),
    );
  }

  int toJson() => value;

  @override
  String toString() => value.toString();
}

{{/isLong}}
{{^isLong}}
/// Typed enum for {{classname}}.{{name}}.
enum {{classname}}{{enumName}} {
{{#allowableValues}}
{{#enumVars}}
  /// Represents the value {{{value}}}.
  {{nameLowercase}}({{{value}}}),
{{/enumVars}}
{{/allowableValues}}
  ;

  final String value;
  const {{classname}}{{enumName}}(this.value);

  /// Returns the enum value matching the given [value], or throws.
  static {{classname}}{{enumName}} fromJson(String value) {
    return {{classname}}{{enumName}}.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown {{classname}}{{enumName}} value: $value'),
    );
  }

  String toJson() => value;

  @override
  String toString() => value;
}

{{/isLong}}
{{/isInteger}}
{{/isEnum}}
{{/vars}}
class {{classname}} {
{{#vars}}
{{#description}}
  /// {{{.}}}
{{/description}}
{{#example}}
  /// Example: `{{{.}}}`
{{/example}}
{{#vendorExtensions.pluralExamples}}
  /// ## {{summary}}
  /// {{description}}
  /// ```json
  /// {{value}}
  /// ```
{{/vendorExtensions.pluralExamples}}
{{#deprecated}}
  @Deprecated('This property is deprecated.')
{{/deprecated}}
{{#required}}
  final {{#isEnum}}{{classname}}{{enumName}}{{/isEnum}}{{^isEnum}}{{{dataType}}}{{/isEnum}} {{name}};
{{/required}}
{{^required}}
  final {{#isEnum}}{{classname}}{{enumName}}{{/isEnum}}{{^isEnum}}{{{dataType}}}{{/isEnum}}? {{name}};
{{/required}}

{{/vars}}
{{#isAdditionalPropertiesTrue}}
  /// Additional properties not defined in the schema.
  final Map<String, dynamic> additionalProperties;

{{/isAdditionalPropertiesTrue}}
{{#hasVars}}
  const {{classname}}({
{{#vars}}
{{#required}}
{{#isEnum}}
    required this.{{name}},
{{/isEnum}}
{{^isEnum}}
{{#defaultValue}}
    this.{{name}} = {{{defaultValue}}},
{{/defaultValue}}
{{^defaultValue}}
    required this.{{name}},
{{/defaultValue}}
{{/isEnum}}
{{/required}}
{{^required}}
{{#isEnum}}
{{#defaultValue}}
    /* Cross-cutting `optional-enum-default-omitted`: an OPTIONAL enum field
     * with a schema `default` is initialised to that default variant so a
     * default-constructed model serialises the default on the wire (parity
     * with the SDKs that emit `"status":"placed"`). The Dart `defaultValue`
     * arrives as the quoted wire literal (e.g. `'placed'`); camelcase strips
     * the quotes to the variant identifier, which equals the lowercased enum
     * value the typed enum declares above. */
    this.{{name}} = {{classname}}{{enumName}}.{{#lambda.camelcase}}{{{defaultValue}}}{{/lambda.camelcase}},
{{/defaultValue}}
{{^defaultValue}}
    this.{{name}},
{{/defaultValue}}
{{/isEnum}}
{{^isEnum}}
{{#defaultValue}}
    this.{{name}} = {{{defaultValue}}},
{{/defaultValue}}
{{^defaultValue}}
    this.{{name}},
{{/defaultValue}}
{{/isEnum}}
{{/required}}
{{/vars}}
{{#isAdditionalPropertiesTrue}}
    this.additionalProperties = const {},
{{/isAdditionalPropertiesTrue}}
  });
{{/hasVars}}
{{^hasVars}}
{{#isAdditionalPropertiesTrue}}
  const {{classname}}({
    this.additionalProperties = const {},
  });
{{/isAdditionalPropertiesTrue}}
{{^isAdditionalPropertiesTrue}}
  const {{classname}}();
{{/isAdditionalPropertiesTrue}}
{{/hasVars}}

  /// Creates a [{{classname}}] from a JSON map.
  factory {{classname}}.fromJson(Map<String, dynamic> json) {
{{#isAdditionalPropertiesTrue}}
    final knownKeys = <String>{
{{#vars}}
      '{{baseName}}',
{{/vars}}
    };
    final additionalProperties = <String, dynamic>{};
    for (final entry in json.entries) {
      if (!knownKeys.contains(entry.key)) {
        additionalProperties[entry.key] = entry.value;
      }
    }
{{/isAdditionalPropertiesTrue}}
{{#isUnevaluatedPropertiesFalse}}
    /* Gap AX.1 — OAS 3.1 / JSON Schema 2020-12 unevaluatedProperties:false.
       Reject any JSON key not declared above. */
    const _declaredKeys = <String>{
{{#vars}}
      '{{baseName}}',
{{/vars}}
    };
    for (final key in json.keys) {
      if (!_declaredKeys.contains(key)) {
        throw ArgumentError(
            "Unknown property '$key' on {{classname}} (unevaluatedProperties:false)");
      }
    }
{{/isUnevaluatedPropertiesFalse}}
{{#vars}}
{{#required}}
    /* DIVERGENCE #10 — a REQUIRED, non-nullable field must be present and
       non-null on the wire. An absent key reads as null in Dart, so this
       single guard covers both the missing and explicitly-null cases. We
       raise the SDK's own SerializationError (not an incidental cast/TypeError)
       so callers see a uniform, catchable wire-shape failure. */
    if (json['{{baseName}}'] == null) {
      throw SerializationError(
          "Missing required field '{{baseName}}' for {{classname}}");
    }
{{/required}}
{{/vars}}
    return {{classname}}(
{{#vars}}
{{#required}}
{{#isEnum}}
{{#isInteger}}
      {{name}}: json['{{baseName}}'] != null ? {{classname}}{{enumName}}.fromJson(json['{{baseName}}'] as int) : throw ArgumentError('Missing required enum field {{baseName}}'),
{{/isInteger}}
{{^isInteger}}
{{#isLong}}
      {{name}}: json['{{baseName}}'] != null ? {{classname}}{{enumName}}.fromJson(json['{{baseName}}'] as int) : throw ArgumentError('Missing required enum field {{baseName}}'),
{{/isLong}}
{{^isLong}}
      {{name}}: json['{{baseName}}'] != null ? {{classname}}{{enumName}}.fromJson(json['{{baseName}}'] as String) : throw ArgumentError('Missing required enum field {{baseName}}'),
{{/isLong}}
{{/isInteger}}
{{/isEnum}}
{{^isEnum}}
{{#isArray}}
      {{name}}: (json['{{baseName}}'] as List).map((e) => {{#items}}{{#isByteArray}}base64Decode(e as String){{/isByteArray}}{{^isByteArray}}{{#isPrimitiveType}}e as {{{dataType}}}{{/isPrimitiveType}}{{^isPrimitiveType}}{{{dataType}}}.fromJson(e as Map<String, dynamic>){{/isPrimitiveType}}{{/isByteArray}}{{/items}}).{{#uniqueItems}}toSet(){{/uniqueItems}}{{^uniqueItems}}toList(){{/uniqueItems}},
{{/isArray}}
{{^isArray}}
{{#isMap}}
{{#items}}
{{#isPrimitiveType}}
      {{name}}: (json['{{baseName}}'] as Map<String, dynamic>).cast<String, {{{dataType}}}>(),
{{/isPrimitiveType}}
{{^isPrimitiveType}}
      {{name}}: (json['{{baseName}}'] as Map<String, dynamic>).map(
        (k, v) => MapEntry(k, {{{dataType}}}.fromJson(v as Map<String, dynamic>)),
      ),
{{/isPrimitiveType}}
{{/items}}
{{/isMap}}
{{^isMap}}
{{#isByteArray}}
      {{name}}: base64Decode(json['{{baseName}}'] as String),
{{/isByteArray}}
{{^isByteArray}}
{{#isPrimitiveType}}
{{#isDateTime}}
      {{name}}: DateTime.parse(json['{{baseName}}'] as String),
{{/isDateTime}}
{{#isDate}}
      {{name}}: json['{{baseName}}'] as String,
{{/isDate}}
{{#isUuid}}
      {{name}}: UuidValue.fromString(json['{{baseName}}'] as String),
{{/isUuid}}
{{#isDuration}}
      {{name}}: parseIso8601Duration(json['{{baseName}}'] as String),
{{/isDuration}}
{{^isDateTime}}{{^isDate}}{{^isUuid}}{{^isDuration}}
      {{name}}: json['{{baseName}}'] as {{{dataType}}},
{{/isDuration}}{{/isUuid}}{{/isDate}}{{/isDateTime}}
{{/isPrimitiveType}}
{{^isPrimitiveType}}
{{#isEnumRef}}
      {{name}}: {{{dataType}}}.fromJson(json['{{baseName}}'] as {{#isInteger}}int{{/isInteger}}{{#isLong}}int{{/isLong}}{{^isInteger}}{{^isLong}}String{{/isLong}}{{/isInteger}}),
{{/isEnumRef}}
{{^isEnumRef}}
      {{name}}: {{{dataType}}}.fromJson(json['{{baseName}}'] as Map<String, dynamic>),
{{/isEnumRef}}
{{/isPrimitiveType}}
{{/isByteArray}}
{{/isMap}}
{{/isArray}}
{{/isEnum}}
{{/required}}
{{^required}}
{{#isEnum}}
{{#isInteger}}
      {{name}}: json['{{baseName}}'] != null ? {{classname}}{{enumName}}.fromJson(json['{{baseName}}'] as int) : null,
{{/isInteger}}
{{^isInteger}}
{{#isLong}}
      {{name}}: json['{{baseName}}'] != null ? {{classname}}{{enumName}}.fromJson(json['{{baseName}}'] as int) : null,
{{/isLong}}
{{^isLong}}
      {{name}}: json['{{baseName}}'] != null ? {{classname}}{{enumName}}.fromJson(json['{{baseName}}'] as String) : null,
{{/isLong}}
{{/isInteger}}
{{/isEnum}}
{{^isEnum}}
{{#isArray}}
      {{name}}: (json['{{baseName}}'] as List?)?.map((e) => {{#items}}{{#isByteArray}}base64Decode(e as String){{/isByteArray}}{{^isByteArray}}{{#isPrimitiveType}}e as {{{dataType}}}{{/isPrimitiveType}}{{^isPrimitiveType}}{{{dataType}}}.fromJson(e as Map<String, dynamic>){{/isPrimitiveType}}{{/isByteArray}}{{/items}}).{{#uniqueItems}}toSet(){{/uniqueItems}}{{^uniqueItems}}toList(){{/uniqueItems}},
{{/isArray}}
{{^isArray}}
{{#isMap}}
{{#items}}
{{#isPrimitiveType}}
      {{name}}: (json['{{baseName}}'] as Map<String, dynamic>?)?.cast<String, {{{dataType}}}>(),
{{/isPrimitiveType}}
{{^isPrimitiveType}}
      {{name}}: (json['{{baseName}}'] as Map<String, dynamic>?)?.map(
        (k, v) => MapEntry(k, {{{dataType}}}.fromJson(v as Map<String, dynamic>)),
      ),
{{/isPrimitiveType}}
{{/items}}
{{/isMap}}
{{^isMap}}
{{#isByteArray}}
      {{name}}: json['{{baseName}}'] != null ? base64Decode(json['{{baseName}}'] as String) : null,
{{/isByteArray}}
{{^isByteArray}}
{{#isPrimitiveType}}
{{#isDateTime}}
      {{name}}: json['{{baseName}}'] != null ? DateTime.parse(json['{{baseName}}'] as String) : null,
{{/isDateTime}}
{{#isDate}}
      {{name}}: json['{{baseName}}'] as String?,
{{/isDate}}
{{#isUuid}}
      {{name}}: json['{{baseName}}'] != null ? UuidValue.fromString(json['{{baseName}}'] as String) : null,
{{/isUuid}}
{{#isDuration}}
      {{name}}: json['{{baseName}}'] != null ? parseIso8601Duration(json['{{baseName}}'] as String) : null,
{{/isDuration}}
{{^isDateTime}}{{^isDate}}{{^isUuid}}{{^isDuration}}
{{#defaultValue}}
      {{name}}: json['{{baseName}}'] != null ? json['{{baseName}}'] as {{{dataType}}} : {{{defaultValue}}},
{{/defaultValue}}
{{^defaultValue}}
      {{name}}: json['{{baseName}}'] as {{{dataType}}}?,
{{/defaultValue}}
{{/isDuration}}{{/isUuid}}{{/isDate}}{{/isDateTime}}
{{/isPrimitiveType}}
{{^isPrimitiveType}}
{{#isEnumRef}}
{{#defaultValue}}
      {{name}}: json['{{baseName}}'] != null ? {{{dataType}}}.fromJson(json['{{baseName}}'] as {{#isInteger}}int{{/isInteger}}{{#isLong}}int{{/isLong}}{{^isInteger}}{{^isLong}}String{{/isLong}}{{/isInteger}}) : {{{defaultValue}}},
{{/defaultValue}}
{{^defaultValue}}
      {{name}}: json['{{baseName}}'] != null ? {{{dataType}}}.fromJson(json['{{baseName}}'] as {{#isInteger}}int{{/isInteger}}{{#isLong}}int{{/isLong}}{{^isInteger}}{{^isLong}}String{{/isLong}}{{/isInteger}}) : null,
{{/defaultValue}}
{{/isEnumRef}}
{{^isEnumRef}}
{{#defaultValue}}
      {{name}}: json['{{baseName}}'] != null ? {{{dataType}}}.fromJson(json['{{baseName}}'] as Map<String, dynamic>) : {{{defaultValue}}},
{{/defaultValue}}
{{^defaultValue}}
      {{name}}: json['{{baseName}}'] != null ? {{{dataType}}}.fromJson(json['{{baseName}}'] as Map<String, dynamic>) : null,
{{/defaultValue}}
{{/isEnumRef}}
{{/isPrimitiveType}}
{{/isByteArray}}
{{/isMap}}
{{/isArray}}
{{/isEnum}}
{{/required}}
{{/vars}}
{{#isAdditionalPropertiesTrue}}
      additionalProperties: additionalProperties,
{{/isAdditionalPropertiesTrue}}
    );
  }

  /// Converts this [{{classname}}] to a JSON map.
  Map<String, dynamic> toJson() {
    final json = <String, dynamic>{};
{{#vars}}
{{#required}}
{{#isEnum}}
    json['{{baseName}}'] = {{name}}.toJson();
{{/isEnum}}
{{^isEnum}}
{{#isArray}}
{{#items}}
{{#isByteArray}}
    json['{{baseName}}'] = {{name}}.map((e) => base64Encode(e)).toList();
{{/isByteArray}}
{{^isByteArray}}
{{#isPrimitiveType}}
    json['{{baseName}}'] = {{name}}.toList();
{{/isPrimitiveType}}
{{^isPrimitiveType}}
    json['{{baseName}}'] = {{name}}.map((e) => e.toJson()).toList();
{{/isPrimitiveType}}
{{/isByteArray}}
{{/items}}
{{/isArray}}
{{^isArray}}
{{#isByteArray}}
    json['{{baseName}}'] = base64Encode({{name}});
{{/isByteArray}}
{{^isByteArray}}
{{#isPrimitiveType}}
{{#isDateTime}}
    json['{{baseName}}'] = {{name}}.toUtc().toIso8601String().replaceFirst(RegExp(r'(\.\d+)?Z$'), '+00:00');
{{/isDateTime}}
{{#isDate}}
    json['{{baseName}}'] = {{name}};
{{/isDate}}
{{#isUuid}}
    json['{{baseName}}'] = {{name}}.toString();
{{/isUuid}}
{{#isDuration}}
    json['{{baseName}}'] = formatIso8601Duration({{name}});
{{/isDuration}}
{{^isDateTime}}{{^isDate}}{{^isUuid}}{{^isDuration}}
    json['{{baseName}}'] = {{name}};
{{/isDuration}}{{/isUuid}}{{/isDate}}{{/isDateTime}}
{{/isPrimitiveType}}
{{^isPrimitiveType}}
{{#isMap}}
    json['{{baseName}}'] = {{name}};
{{/isMap}}
{{^isMap}}
    json['{{baseName}}'] = {{name}}.toJson();
{{/isMap}}
{{/isPrimitiveType}}
{{/isByteArray}}
{{/isArray}}
{{/isEnum}}
{{/required}}
{{^required}}
    if ({{name}} != null) {
{{#isEnum}}
      json['{{baseName}}'] = {{name}}?.toJson();
{{/isEnum}}
{{^isEnum}}
{{#isArray}}
{{#items}}
{{#isByteArray}}
      json['{{baseName}}'] = {{name}}?.map((e) => base64Encode(e)).toList();
{{/isByteArray}}
{{^isByteArray}}
{{#isPrimitiveType}}
      json['{{baseName}}'] = {{name}}?.toList();
{{/isPrimitiveType}}
{{^isPrimitiveType}}
      json['{{baseName}}'] = {{name}}?.map((e) => e.toJson()).toList();
{{/isPrimitiveType}}
{{/isByteArray}}
{{/items}}
{{/isArray}}
{{^isArray}}
{{#isByteArray}}
      json['{{baseName}}'] = base64Encode({{name}}!);
{{/isByteArray}}
{{^isByteArray}}
{{#isPrimitiveType}}
{{#isDateTime}}
      json['{{baseName}}'] = {{name}}?.toUtc().toIso8601String().replaceFirst(RegExp(r'(\.\d+)?Z$'), '+00:00');
{{/isDateTime}}
{{#isDate}}
      json['{{baseName}}'] = {{name}};
{{/isDate}}
{{#isUuid}}
      json['{{baseName}}'] = {{name}}?.toString();
{{/isUuid}}
{{#isDuration}}
      json['{{baseName}}'] = formatIso8601Duration({{name}}!);
{{/isDuration}}
{{^isDateTime}}{{^isDate}}{{^isUuid}}{{^isDuration}}
      json['{{baseName}}'] = {{name}};
{{/isDuration}}{{/isUuid}}{{/isDate}}{{/isDateTime}}
{{/isPrimitiveType}}
{{^isPrimitiveType}}
{{#isMap}}
      json['{{baseName}}'] = {{name}};
{{/isMap}}
{{^isMap}}
      json['{{baseName}}'] = {{name}}?.toJson();
{{/isMap}}
{{/isPrimitiveType}}
{{/isByteArray}}
{{/isArray}}
{{/isEnum}}
    }
{{/required}}
{{/vars}}
{{#isAdditionalPropertiesTrue}}
    json.addAll(additionalProperties);
{{/isAdditionalPropertiesTrue}}
    return json;
  }

  /// Value-equality based on all declared fields{{#isAdditionalPropertiesTrue}} (including the
  /// additionalProperties map){{/isAdditionalPropertiesTrue}}. Nested List/Map fields are compared
  /// by reference — callers needing structural equality on those should
  /// use `package:collection`'s `DeepCollectionEquality`.
  @override
  bool operator ==(Object other) {
    if (identical(this, other)) return true;
    return other is {{classname}}{{#vars}} &&
        {{name}} == other.{{name}}{{/vars}}{{#isAdditionalPropertiesTrue}} &&
        _mapEquals(additionalProperties, other.additionalProperties){{/isAdditionalPropertiesTrue}};
  }

  /// hashCode emits Object.hashAll which accepts an arbitrary-length
  /// Iterable (Object.hash requires 2+ positional args, so it can't
  /// represent the 0-var or 1-var cases without special-casing).
  @override
  int get hashCode => Object.hashAll([{{#vars}}{{name}}{{^-last}}, {{/-last}}{{/vars}}{{#isAdditionalPropertiesTrue}}{{#hasVars}}, {{/hasVars}}additionalProperties{{/isAdditionalPropertiesTrue}}]);
{{#isAdditionalPropertiesTrue}}

  static bool _mapEquals(Map<String, dynamic> a, Map<String, dynamic> b) {
    if (identical(a, b)) return true;
    if (a.length != b.length) return false;
    for (final key in a.keys) {
      if (!b.containsKey(key) || a[key] != b[key]) return false;
    }
    return true;
  }
{{/isAdditionalPropertiesTrue}}
}
{{/isPrimitiveAnyOf}}
{{/isPrimitiveOneOf}}
{{#isPrimitiveOneOf}}
{{#description}}
/// {{classname}} {{{.}}}
{{/description}}
{{^description}}
/// {{classname}} is a union type (oneOf) of primitive/array variants.
{{/description}}
{{#isDeprecated}}
@Deprecated('This schema is deprecated.')
{{/isDeprecated}}
class {{classname}} {
  /// The decoded payload. For a `string`/`byte` variant this is a
  /// [Uint8List]; for an array variant a [List]; for an object variant the
  /// generated model instance. Never null once constructed from the wire.
  final Object? _value;

  const {{classname}}._(this._value);

  /// Wraps an already-decoded value. The value must match one of the
  /// declared oneOf variants; it is re-encoded verbatim by [toJson].
  const {{classname}}.value(Object? value) : _value = value;

  /// Returns the underlying value of the union type.
  Object? get value => _value;

  /// Converts this union type back to its JSON wire value.
  Object? toJson() => _encodeValue(_value);

  /// Creates a [{{classname}}] from a decoded JSON value (a base64 [String]
  /// for a `byte` variant, a [List] for an array variant, or a
  /// `Map<String, dynamic>` for an object variant).
  factory {{classname}}.fromJson(Object? json) {
{{#composedSchemas}}
{{#oneOf}}
    try {
      return {{classname}}._(
{{#isByteArray}}
        base64Decode(json as String),
{{/isByteArray}}
{{^isByteArray}}
{{#isArray}}
{{#items}}
{{#isByteArray}}
        (json as List).map((e) => base64Decode(e as String)).{{#uniqueItems}}toSet(){{/uniqueItems}}{{^uniqueItems}}toList(){{/uniqueItems}},
{{/isByteArray}}
{{^isByteArray}}
{{#isPrimitiveType}}
        (json as List).map((e) => e as {{{dataType}}}).{{#uniqueItems}}toSet(){{/uniqueItems}}{{^uniqueItems}}toList(){{/uniqueItems}},
{{/isPrimitiveType}}
{{^isPrimitiveType}}
        (json as List).map((e) => {{{dataType}}}.fromJson(e as Map<String, dynamic>)).{{#uniqueItems}}toSet(){{/uniqueItems}}{{^uniqueItems}}toList(){{/uniqueItems}},
{{/isPrimitiveType}}
{{/isByteArray}}
{{/items}}
{{/isArray}}
{{^isArray}}
{{#isPrimitiveType}}
        json as {{{dataType}}},
{{/isPrimitiveType}}
{{^isPrimitiveType}}
        {{{dataType}}}.fromJson(json as Map<String, dynamic>),
{{/isPrimitiveType}}
{{/isArray}}
{{/isByteArray}}
      );
    } catch (_) {}
{{/oneOf}}
{{/composedSchemas}}
    /* Cross-cutting `oneof-nondiscriminator-no-match-silent`: no variant
       matched the wire shape. Throw a SerializationError rather than
       returning a silently-empty union that drops the payload. */
    throw SerializationError('Data does not match any oneOf schemas for {{classname}}');
  }

  /// Re-encodes a decoded value to its JSON wire form. Byte values are
  /// base64-encoded; generated models delegate to their own toJson; plain
  /// JSON primitives/maps pass through unchanged.
  static Object? _encodeValue(Object? v) {
    if (v == null) return null;
    if (v is Uint8List) return base64Encode(v);
    if (v is List) return v.map(_encodeValue).toList();
    if (v is Map) return v;
    try {
      return (v as dynamic).toJson();
    } catch (_) {
      return v;
    }
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) return true;
    return other is {{classname}} && _value == other._value;
  }

  @override
  int get hashCode => _value.hashCode;
}
{{/isPrimitiveOneOf}}
{{#isPrimitiveAnyOf}}
{{#description}}
/// {{classname}} {{{.}}}
{{/description}}
{{^description}}
/// {{classname}} is a union type (anyOf) of primitive/array variants.
{{/description}}
{{#isDeprecated}}
@Deprecated('This schema is deprecated.')
{{/isDeprecated}}
class {{classname}} {
  /// The decoded payload. For a `string`/`byte` variant this is a
  /// [Uint8List]; for an array variant a [List]; for an object variant the
  /// generated model instance. Never null once constructed from the wire.
  final Object? _value;

  const {{classname}}._(this._value);

  /// Wraps an already-decoded value. The value must match one of the
  /// declared anyOf variants; it is re-encoded verbatim by [toJson].
  const {{classname}}.value(Object? value) : _value = value;

  /// Returns the underlying value of the union type.
  Object? get value => _value;

  /// Converts this union type back to its JSON wire value.
  Object? toJson() => _encodeValue(_value);

  /// Creates a [{{classname}}] from a decoded JSON value (a base64 [String]
  /// for a `byte` variant, a [List] for an array variant, or a
  /// `Map<String, dynamic>` for an object variant).
  factory {{classname}}.fromJson(Object? json) {
{{#composedSchemas}}
{{#anyOf}}
    try {
      return {{classname}}._(
{{#isByteArray}}
        base64Decode(json as String),
{{/isByteArray}}
{{^isByteArray}}
{{#isArray}}
{{#items}}
{{#isByteArray}}
        (json as List).map((e) => base64Decode(e as String)).{{#uniqueItems}}toSet(){{/uniqueItems}}{{^uniqueItems}}toList(){{/uniqueItems}},
{{/isByteArray}}
{{^isByteArray}}
{{#isPrimitiveType}}
        (json as List).map((e) => e as {{{dataType}}}).{{#uniqueItems}}toSet(){{/uniqueItems}}{{^uniqueItems}}toList(){{/uniqueItems}},
{{/isPrimitiveType}}
{{^isPrimitiveType}}
        (json as List).map((e) => {{{dataType}}}.fromJson(e as Map<String, dynamic>)).{{#uniqueItems}}toSet(){{/uniqueItems}}{{^uniqueItems}}toList(){{/uniqueItems}},
{{/isPrimitiveType}}
{{/isByteArray}}
{{/items}}
{{/isArray}}
{{^isArray}}
{{#isPrimitiveType}}
        json as {{{dataType}}},
{{/isPrimitiveType}}
{{^isPrimitiveType}}
        {{{dataType}}}.fromJson(json as Map<String, dynamic>),
{{/isPrimitiveType}}
{{/isArray}}
{{/isByteArray}}
      );
    } catch (_) {}
{{/anyOf}}
{{/composedSchemas}}
    /* Cross-cutting `oneof-nondiscriminator-no-match-silent`: no variant
       matched the wire shape — surface a SerializationError rather than
       a silently-empty union that drops the payload. */
    throw SerializationError('Data does not match any anyOf schemas for {{classname}}');
  }

  /// Re-encodes a decoded value to its JSON wire form. Byte values are
  /// base64-encoded; generated models delegate to their own toJson; plain
  /// JSON primitives/maps pass through unchanged.
  static Object? _encodeValue(Object? v) {
    if (v == null) return null;
    if (v is Uint8List) return base64Encode(v);
    if (v is List) return v.map(_encodeValue).toList();
    if (v is Map) return v;
    try {
      return (v as dynamic).toJson();
    } catch (_) {
      return v;
    }
  }

  @override
  bool operator ==(Object other) {
    if (identical(this, other)) return true;
    return other is {{classname}} && _value == other._value;
  }

  @override
  int get hashCode => _value.hashCode;
}
{{/isPrimitiveAnyOf}}
{{/anyOf}}
{{/oneOf}}
{{#oneOf}}
{{#-first}}
{{#description}}
/// {{classname}} {{{.}}}
{{/description}}
{{^description}}
/// {{classname}} is a union type (oneOf).
{{/description}}
{{#isDeprecated}}
@Deprecated('This schema is deprecated.')
{{/isDeprecated}}
class {{classname}} {
  final Object? _value;

  const {{classname}}._(this._value);

  /// Returns the underlying value of the union type.
  Object? get value => _value;

  /// Converts this union type to a JSON value.
  Object? toJson() {
    final v = _value;
    if (v == null) return null;
    if (v is Map) return v;
    try {
      return (v as dynamic).toJson();
    } catch (_) {
      return v;
    }
  }

  /// Creates a [{{classname}}] from a JSON value.
  factory {{classname}}.fromJson(Map<String, dynamic> json) {
{{#discriminator}}
    final disc = json['{{propertyBaseName}}'];
    switch (disc) {
{{#mappedModels}}
      case '{{{mappingName}}}':
        return {{classname}}._({{{modelName}}}.fromJson(json));
{{/mappedModels}}
      default:
        /* 4.7 — discriminator value not in the listed mappings.
           Throw a SerializationError instead of falling through, so
           callers can distinguish wire-shape failures from generic
           argument errors. */
        throw SerializationError(
            'Unknown discriminator value for {{classname}}: $disc');
    }
{{/discriminator}}
{{^discriminator}}
{{#oneOf}}
    try {
      return {{classname}}._({{{.}}}.fromJson(json));
    } catch (_) {}
{{/oneOf}}
    /* Cross-cutting `oneof-nondiscriminator-no-match-silent`: no variant
       matched the wire shape. Throw a SerializationError (the same family
       as the discriminator no-match above) so the caller sees a wire-shape
       failure instead of a silently-empty union. */
    throw SerializationError('Data does not match any oneOf schemas for {{classname}}');
{{/discriminator}}
  }
}
{{/-first}}
{{/oneOf}}
{{#anyOf}}
{{#-first}}
{{#description}}
/// {{classname}} {{{.}}}
{{/description}}
{{^description}}
/// {{classname}} is a union type (anyOf).
{{/description}}
{{#isDeprecated}}
@Deprecated('This schema is deprecated.')
{{/isDeprecated}}
class {{classname}} {
  final Object? _value;

  const {{classname}}._(this._value);

  /// Returns the underlying value of the union type.
  Object? get value => _value;

  /// Converts this union type to a JSON value.
  Object? toJson() {
    final v = _value;
    if (v == null) return null;
    if (v is Map) return v;
    try {
      return (v as dynamic).toJson();
    } catch (_) {
      return v;
    }
  }

  /// Creates a [{{classname}}] from a JSON value.
  factory {{classname}}.fromJson(Map<String, dynamic> json) {
{{#anyOf}}
    try {
      return {{classname}}._({{{.}}}.fromJson(json));
    } catch (_) {}
{{/anyOf}}
    /* Cross-cutting `oneof-nondiscriminator-no-match-silent`: no variant
       matched the wire shape — surface a SerializationError rather than
       a silently-empty union. */
    throw SerializationError('Data does not match any anyOf schemas for {{classname}}');
  }
}
{{/-first}}
{{/anyOf}}
{{/isEnum}}
{{/model}}
{{/models}}
