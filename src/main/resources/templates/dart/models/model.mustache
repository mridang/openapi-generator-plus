{{>api_info}}
// ignore_for_file: unused_import, no_leading_underscores_for_local_identifiers, unintended_html_in_doc_comment
{{#models}}
{{#model}}
import 'dart:convert';
import 'dart:typed_data';

import 'package:collection/collection.dart';

import '../object_serializer.dart';

{{#hasDartImports}}
{{#dartImports}}
import '{{filename}}.dart';
{{/dartImports}}

{{/hasDartImports}}
{{#isEnum}}
{{#isInteger}}
{{#description}}
/// {{#lambda.unescapeDocComment}}{{{description}}}{{/lambda.unescapeDocComment}}
{{/description}}
{{^description}}
/// {{classname}} is an enumeration of allowed integer values.
{{/description}}
enum {{classname}} {
{{#allowableValues}}
{{#enumVars}}
  /// Represents the value {{{value}}}.
  {{name}}({{{value}}}),
{{/enumVars}}
{{/allowableValues}}
  ;

  final int value;
  const {{classname}}(this.value);

  /// Returns the enum value matching the given [value], or throws.
  static {{classname}} fromJson(int value) {
    return {{classname}}.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown {{classname}} value: $value'),
    );
  }

  int toJson() => value;

  @override
  String toString() => value.toString();
}
{{/isInteger}}
{{^isInteger}}
{{#isLong}}
{{#description}}
/// {{#lambda.unescapeDocComment}}{{{description}}}{{/lambda.unescapeDocComment}}
{{/description}}
{{^description}}
/// {{classname}} is an enumeration of allowed integer values.
{{/description}}
enum {{classname}} {
{{#allowableValues}}
{{#enumVars}}
  /// Represents the value {{{value}}}.
  {{name}}({{{value}}}),
{{/enumVars}}
{{/allowableValues}}
  ;

  final int value;
  const {{classname}}(this.value);

  /// Returns the enum value matching the given [value], or throws.
  static {{classname}} fromJson(int value) {
    return {{classname}}.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown {{classname}} value: $value'),
    );
  }

  int toJson() => value;

  @override
  String toString() => value.toString();
}
{{/isLong}}
{{^isLong}}
{{#description}}
/// {{#lambda.unescapeDocComment}}{{{description}}}{{/lambda.unescapeDocComment}}
{{/description}}
{{^description}}
/// {{classname}} is an enumeration of allowed values.
{{/description}}
enum {{classname}} {
{{#allowableValues}}
{{#enumVars}}
  /// Represents the value {{{value}}}.
  {{name}}({{{value}}}),
{{/enumVars}}
{{/allowableValues}}
  ;

  final String value;
  const {{classname}}(this.value);

  /// Returns the enum value matching the given [value], or throws.
  static {{classname}} fromJson(String value) {
    return {{classname}}.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown {{classname}} value: $value'),
    );
  }

  String toJson() => value;

  @override
  String toString() => value;
}
{{/isLong}}
{{/isInteger}}
{{/isEnum}}
{{^isEnum}}
{{^oneOf}}
{{^anyOf}}
{{^isPrimitiveOneOf}}
{{^isPrimitiveAnyOf}}
{{#description}}
/// {{classname}} {{#lambda.unescapeDocComment}}{{{.}}}{{/lambda.unescapeDocComment}}
{{/description}}
{{^description}}
/// {{classname}} is a model class generated from the OpenAPI schema.
{{/description}}
{{#isDeprecated}}
@Deprecated('This schema is deprecated.')
{{/isDeprecated}}
{{#vars}}
{{#isEnum}}
{{#isInteger}}
/// Typed integer enum for {{classname}}.{{name}}.
enum {{classname}}{{enumName}} {
{{#allowableValues}}
{{#enumVars}}
  /// Represents the value {{{value}}}.
  {{nameLowercase}}({{{value}}}),
{{/enumVars}}
{{/allowableValues}}
  ;

  final int value;
  const {{classname}}{{enumName}}(this.value);

  /// Returns the enum value matching the given [value], or throws.
  static {{classname}}{{enumName}} fromJson(int value) {
    return {{classname}}{{enumName}}.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown {{classname}}{{enumName}} value: $value'),
    );
  }

  int toJson() => value;

  @override
  String toString() => value.toString();
}

{{/isInteger}}
{{^isInteger}}
{{#isLong}}
/// Typed integer enum for {{classname}}.{{name}}.
enum {{classname}}{{enumName}} {
{{#allowableValues}}
{{#enumVars}}
  /// Represents the value {{{value}}}.
  {{nameLowercase}}({{{value}}}),
{{/enumVars}}
{{/allowableValues}}
  ;

  final int value;
  const {{classname}}{{enumName}}(this.value);

  /// Returns the enum value matching the given [value], or throws.
  static {{classname}}{{enumName}} fromJson(int value) {
    return {{classname}}{{enumName}}.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown {{classname}}{{enumName}} value: $value'),
    );
  }

  int toJson() => value;

  @override
  String toString() => value.toString();
}

{{/isLong}}
{{^isLong}}
/// Typed enum for {{classname}}.{{name}}.
enum {{classname}}{{enumName}} {
{{#allowableValues}}
{{#enumVars}}
  /// Represents the value {{{value}}}.
  {{nameLowercase}}({{{value}}}),
{{/enumVars}}
{{/allowableValues}}
  ;

  final String value;
  const {{classname}}{{enumName}}(this.value);

  /// Returns the enum value matching the given [value], or throws.
  static {{classname}}{{enumName}} fromJson(String value) {
    return {{classname}}{{enumName}}.values.firstWhere(
      (e) => e.value == value,
      orElse: () => throw ArgumentError('Unknown {{classname}}{{enumName}} value: $value'),
    );
  }

  String toJson() => value;

  @override
  String toString() => value;
}

{{/isLong}}
{{/isInteger}}
{{/isEnum}}
{{/vars}}
class {{classname}} {
{{#vars}}
{{#description}}
  /// {{#lambda.unescapeDocComment}}{{{.}}}{{/lambda.unescapeDocComment}}
{{/description}}
{{#example}}
  /// Example: `{{{.}}}`
{{/example}}
{{#vendorExtensions.pluralExamples}}
  /// ## {{summary}}
  /// {{description}}
  /// ```json
  /// {{value}}
  /// ```
{{/vendorExtensions.pluralExamples}}
{{#deprecated}}
  @Deprecated('This property is deprecated.')
{{/deprecated}}
{{#required}}
  final {{#isEnum}}{{classname}}{{enumName}}{{/isEnum}}{{^isEnum}}{{{dataType}}}{{/isEnum}} {{name}};
{{/required}}
{{^required}}
  final {{#isEnum}}{{classname}}{{enumName}}{{/isEnum}}{{^isEnum}}{{{dataType}}}{{/isEnum}}? {{name}};
{{/required}}

{{/vars}}
{{#isAdditionalPropertiesTrue}}
  /// Additional properties not defined in the schema.
  final Map<String, dynamic> additionalProperties;

{{/isAdditionalPropertiesTrue}}
{{#hasVars}}
  const {{classname}}({
{{#vars}}
{{#required}}
{{#isEnum}}
    required this.{{name}},
{{/isEnum}}
{{^isEnum}}
{{#defaultValue}}
    this.{{name}} = {{{defaultValue}}},
{{/defaultValue}}
{{^defaultValue}}
    required this.{{name}},
{{/defaultValue}}
{{/isEnum}}
{{/required}}
{{^required}}
{{#isEnum}}
{{#defaultValue}}
    /* Cross-cutting `optional-enum-default-omitted`: an OPTIONAL enum field
     * with a schema `default` is initialised to that default variant so a
     * default-constructed model serialises the default on the wire (parity
     * with the SDKs that emit `"status":"placed"`). `defaultEnumVarName` is
     * the `nameLowercase` of the variant whose wire value equals the schema
     * default — i.e. the exact identifier the typed enum declares above — so
     * a non-lowercase wire value (e.g. `IN_STOCK`) still references a real
     * variant rather than a re-camelcased copy of the literal. */
    this.{{name}} = {{classname}}{{enumName}}.{{vendorExtensions.defaultEnumVarName}},
{{/defaultValue}}
{{^defaultValue}}
    this.{{name}},
{{/defaultValue}}
{{/isEnum}}
{{^isEnum}}
{{#defaultValue}}
    this.{{name}} = {{{defaultValue}}},
{{/defaultValue}}
{{^defaultValue}}
    this.{{name}},
{{/defaultValue}}
{{/isEnum}}
{{/required}}
{{/vars}}
{{#isAdditionalPropertiesTrue}}
    this.additionalProperties = const {},
{{/isAdditionalPropertiesTrue}}
  });
{{/hasVars}}
{{^hasVars}}
{{#isAdditionalPropertiesTrue}}
  const {{classname}}({
    this.additionalProperties = const {},
  });
{{/isAdditionalPropertiesTrue}}
{{^isAdditionalPropertiesTrue}}
  const {{classname}}();
{{/isAdditionalPropertiesTrue}}
{{/hasVars}}

  /// Creates a [{{classname}}] from a JSON map.
  factory {{classname}}.fromJson(Map<String, dynamic> json) {
{{#isAdditionalPropertiesTrue}}
    final knownKeys = <String>{
{{#vars}}
      '{{baseName}}',
{{/vars}}
    };
    final additionalProperties = <String, dynamic>{};
    for (final entry in json.entries) {
      if (!knownKeys.contains(entry.key)) {
        additionalProperties[entry.key] = entry.value;
      }
    }
{{/isAdditionalPropertiesTrue}}
{{#isUnevaluatedPropertiesFalse}}
    /* Gap AX.1 — OAS 3.1 / JSON Schema 2020-12 unevaluatedProperties:false.
       Reject any JSON key not declared above. */
    const _declaredKeys = <String>{
{{#vars}}
      '{{baseName}}',
{{/vars}}
    };
    for (final key in json.keys) {
      if (!_declaredKeys.contains(key)) {
        throw ArgumentError(
            "Unknown property '$key' on {{classname}} (unevaluatedProperties:false)");
      }
    }
{{/isUnevaluatedPropertiesFalse}}
{{#vars}}
{{#required}}
    /* DIVERGENCE #10 — a REQUIRED, non-nullable field must be present and
       non-null on the wire. An absent key reads as null in Dart, so this
       single guard covers both the missing and explicitly-null cases. We
       raise the SDK's own SerializationError (not an incidental cast/TypeError)
       so callers see a uniform, catchable wire-shape failure. */
    if (json['{{baseName}}'] == null) {
      throw SerializationError(
          "Missing required field '{{baseName}}' for {{classname}}");
    }
{{/required}}
{{/vars}}
    return {{classname}}(
{{#vars}}
{{#required}}
{{#isEnum}}
{{#isInteger}}
      {{name}}: json['{{baseName}}'] != null ? {{classname}}{{enumName}}.fromJson(json['{{baseName}}'] as int) : throw ArgumentError('Missing required enum field {{baseName}}'),
{{/isInteger}}
{{^isInteger}}
{{#isLong}}
      {{name}}: json['{{baseName}}'] != null ? {{classname}}{{enumName}}.fromJson(json['{{baseName}}'] as int) : throw ArgumentError('Missing required enum field {{baseName}}'),
{{/isLong}}
{{^isLong}}
      {{name}}: json['{{baseName}}'] != null ? {{classname}}{{enumName}}.fromJson(json['{{baseName}}'] as String) : throw ArgumentError('Missing required enum field {{baseName}}'),
{{/isLong}}
{{/isInteger}}
{{/isEnum}}
{{^isEnum}}
{{#isArray}}
      {{! recursive-container-deserialize: route each element through the }}
      {{! deserialize_arg partial so a nested container (List<Map<..>>, }}
      {{! List<List<..>>) or an enum/date/byte leaf is decoded at the correct }}
      {{! depth instead of one level deep. A primitive `Map<String, X>` value }}
      {{! that is itself primitive still casts; deserializeArray returns a }}
      {{! List which `.toSet()` narrows for uniqueItems. }}
      {{name}}: deserializeArray(json['{{baseName}}'] as List, {{#items}}{{>deserialize_arg}}{{/items}}){{#uniqueItems}}.toSet(){{/uniqueItems}},
{{/isArray}}
{{^isArray}}
{{#isMap}}
{{#items}}
{{#isPrimitiveType}}
      {{name}}: (json['{{baseName}}'] as Map<String, dynamic>).cast<String, {{{dataType}}}>(),
{{/isPrimitiveType}}
{{^isPrimitiveType}}
      {{name}}: deserializeMap(json['{{baseName}}'] as Map<String, dynamic>, {{>deserialize_arg}}),
{{/isPrimitiveType}}
{{/items}}
{{/isMap}}
{{^isMap}}
{{#isByteArray}}
      {{name}}: base64Decode(json['{{baseName}}'] as String),
{{/isByteArray}}
{{^isByteArray}}
{{#isPrimitiveType}}
{{#isDateTime}}
      {{name}}: DateTime.parse(json['{{baseName}}'] as String),
{{/isDateTime}}
{{#isDate}}
      {{name}}: json['{{baseName}}'] as String,
{{/isDate}}
{{#isUuid}}
      {{name}}: UuidValue.fromString(json['{{baseName}}'] as String),
{{/isUuid}}
{{#vendorExtensions.isDuration}}
      {{name}}: parseProtobufDuration(json['{{baseName}}'] as String),
{{/vendorExtensions.isDuration}}
{{^isDateTime}}{{^isDate}}{{^isUuid}}{{^vendorExtensions.isDuration}}
{{! A `double`-typed field (type:number, or format:float / format:double) must
    accept an integral JSON value (e.g. 5 vs 5.0): `as double` throws on an int,
    so coerce via num.toDouble(). `number`, `float` and `double` all map to the
    Dart `double` type but surface as distinct codegen flags, so each must be
    handled; only a genuinely non-numeric primitive falls through to a plain
    cast. }}
{{#isNumber}}
      {{name}}: (json['{{baseName}}'] as num).toDouble(),
{{/isNumber}}
{{^isNumber}}
{{#isFloat}}
      {{name}}: (json['{{baseName}}'] as num).toDouble(),
{{/isFloat}}
{{^isFloat}}
{{#isDouble}}
      {{name}}: (json['{{baseName}}'] as num).toDouble(),
{{/isDouble}}
{{^isDouble}}
      {{name}}: json['{{baseName}}'] as {{{dataType}}},
{{/isDouble}}
{{/isFloat}}
{{/isNumber}}
{{/vendorExtensions.isDuration}}{{/isUuid}}{{/isDate}}{{/isDateTime}}
{{/isPrimitiveType}}
{{^isPrimitiveType}}
{{#isEnumRef}}
      {{name}}: {{{dataType}}}.fromJson(json['{{baseName}}'] as {{#isInteger}}int{{/isInteger}}{{#isLong}}int{{/isLong}}{{^isInteger}}{{^isLong}}String{{/isLong}}{{/isInteger}}),
{{/isEnumRef}}
{{^isEnumRef}}
      {{name}}: {{{dataType}}}.fromJson(json['{{baseName}}'] as Map<String, dynamic>),
{{/isEnumRef}}
{{/isPrimitiveType}}
{{/isByteArray}}
{{/isMap}}
{{/isArray}}
{{/isEnum}}
{{/required}}
{{^required}}
{{#isEnum}}
{{#isInteger}}
{{#defaultValue}}
      /* Cross-cutting `default-applies-only-when-absent`: apply the enum
         default solely when the key is OMITTED; an explicit `null` is
         preserved as null per the JSON-Schema contract. */
      {{name}}: json.containsKey('{{baseName}}') ? (json['{{baseName}}'] != null ? {{classname}}{{enumName}}.fromJson(json['{{baseName}}'] as int) : null) : {{classname}}{{enumName}}.{{vendorExtensions.defaultEnumVarName}},
{{/defaultValue}}
{{^defaultValue}}
      {{name}}: json['{{baseName}}'] != null ? {{classname}}{{enumName}}.fromJson(json['{{baseName}}'] as int) : null,
{{/defaultValue}}
{{/isInteger}}
{{^isInteger}}
{{#isLong}}
{{#defaultValue}}
      /* Cross-cutting `default-applies-only-when-absent`: apply the enum
         default solely when the key is OMITTED; an explicit `null` is
         preserved as null per the JSON-Schema contract. */
      {{name}}: json.containsKey('{{baseName}}') ? (json['{{baseName}}'] != null ? {{classname}}{{enumName}}.fromJson(json['{{baseName}}'] as int) : null) : {{classname}}{{enumName}}.{{vendorExtensions.defaultEnumVarName}},
{{/defaultValue}}
{{^defaultValue}}
      {{name}}: json['{{baseName}}'] != null ? {{classname}}{{enumName}}.fromJson(json['{{baseName}}'] as int) : null,
{{/defaultValue}}
{{/isLong}}
{{^isLong}}
{{#defaultValue}}
      /* Cross-cutting `default-applies-only-when-absent`: apply the enum
         default solely when the key is OMITTED; an explicit `null` is
         preserved as null per the JSON-Schema contract. */
      {{name}}: json.containsKey('{{baseName}}') ? (json['{{baseName}}'] != null ? {{classname}}{{enumName}}.fromJson(json['{{baseName}}'] as String) : null) : {{classname}}{{enumName}}.{{vendorExtensions.defaultEnumVarName}},
{{/defaultValue}}
{{^defaultValue}}
      {{name}}: json['{{baseName}}'] != null ? {{classname}}{{enumName}}.fromJson(json['{{baseName}}'] as String) : null,
{{/defaultValue}}
{{/isLong}}
{{/isInteger}}
{{/isEnum}}
{{^isEnum}}
{{#isArray}}
      {{! recursive-container-deserialize (optional): same recursive descent as }}
      {{! the required branch, guarded for an absent/null key. }}
      {{name}}: json['{{baseName}}'] != null ? deserializeArray(json['{{baseName}}'] as List, {{#items}}{{>deserialize_arg}}{{/items}}){{#uniqueItems}}.toSet(){{/uniqueItems}} : null,
{{/isArray}}
{{^isArray}}
{{#isMap}}
{{#items}}
{{#isPrimitiveType}}
      {{name}}: (json['{{baseName}}'] as Map<String, dynamic>?)?.cast<String, {{{dataType}}}>(),
{{/isPrimitiveType}}
{{^isPrimitiveType}}
      {{name}}: json['{{baseName}}'] != null ? deserializeMap(json['{{baseName}}'] as Map<String, dynamic>, {{>deserialize_arg}}) : null,
{{/isPrimitiveType}}
{{/items}}
{{/isMap}}
{{^isMap}}
{{#isByteArray}}
      {{name}}: json['{{baseName}}'] != null ? base64Decode(json['{{baseName}}'] as String) : null,
{{/isByteArray}}
{{^isByteArray}}
{{#isPrimitiveType}}
{{#isDateTime}}
      {{name}}: json['{{baseName}}'] != null ? DateTime.parse(json['{{baseName}}'] as String) : null,
{{/isDateTime}}
{{#isDate}}
      {{name}}: json['{{baseName}}'] as String?,
{{/isDate}}
{{#isUuid}}
      {{name}}: json['{{baseName}}'] != null ? UuidValue.fromString(json['{{baseName}}'] as String) : null,
{{/isUuid}}
{{#vendorExtensions.isDuration}}
      {{name}}: json['{{baseName}}'] != null ? parseProtobufDuration(json['{{baseName}}'] as String) : null,
{{/vendorExtensions.isDuration}}
{{^isDateTime}}{{^isDate}}{{^isUuid}}{{^vendorExtensions.isDuration}}
{{#defaultValue}}
      /* Cross-cutting `default-applies-only-when-absent`: a schema `default`
         fills in only when the key is OMITTED from the payload. An explicit
         JSON `null` is a provided value and is preserved as null (not replaced
         by the default), matching the JSON-Schema contract. */
      {{name}}: json.containsKey('{{baseName}}') ? {{#isNumber}}(json['{{baseName}}'] as num?)?.toDouble(){{/isNumber}}{{^isNumber}}{{#isFloat}}(json['{{baseName}}'] as num?)?.toDouble(){{/isFloat}}{{^isFloat}}{{#isDouble}}(json['{{baseName}}'] as num?)?.toDouble(){{/isDouble}}{{^isDouble}}json['{{baseName}}'] as {{{dataType}}}?{{/isDouble}}{{/isFloat}}{{/isNumber}} : {{{defaultValue}}},
{{/defaultValue}}
{{^defaultValue}}
{{! A `double`-typed field (type:number, or format:float / format:double) must
    accept an integral JSON value: coerce via num. The three flags are distinct
    but all map to the Dart `double` type, so each routes through toDouble();
    only a non-numeric primitive falls through to a plain nullable cast. }}
{{#isNumber}}
      {{name}}: (json['{{baseName}}'] as num?)?.toDouble(),
{{/isNumber}}
{{^isNumber}}
{{#isFloat}}
      {{name}}: (json['{{baseName}}'] as num?)?.toDouble(),
{{/isFloat}}
{{^isFloat}}
{{#isDouble}}
      {{name}}: (json['{{baseName}}'] as num?)?.toDouble(),
{{/isDouble}}
{{^isDouble}}
      {{name}}: json['{{baseName}}'] as {{{dataType}}}?,
{{/isDouble}}
{{/isFloat}}
{{/isNumber}}
{{/defaultValue}}
{{/vendorExtensions.isDuration}}{{/isUuid}}{{/isDate}}{{/isDateTime}}
{{/isPrimitiveType}}
{{^isPrimitiveType}}
{{#isEnumRef}}
{{#defaultValue}}
      /* Cross-cutting `default-applies-only-when-absent`: apply the default
         solely when the key is OMITTED; an explicit `null` is preserved as
         null per the JSON-Schema contract. */
      {{name}}: json.containsKey('{{baseName}}') ? (json['{{baseName}}'] != null ? {{{dataType}}}.fromJson(json['{{baseName}}'] as {{#isInteger}}int{{/isInteger}}{{#isLong}}int{{/isLong}}{{^isInteger}}{{^isLong}}String{{/isLong}}{{/isInteger}}) : null) : {{{defaultValue}}},
{{/defaultValue}}
{{^defaultValue}}
      {{name}}: json['{{baseName}}'] != null ? {{{dataType}}}.fromJson(json['{{baseName}}'] as {{#isInteger}}int{{/isInteger}}{{#isLong}}int{{/isLong}}{{^isInteger}}{{^isLong}}String{{/isLong}}{{/isInteger}}) : null,
{{/defaultValue}}
{{/isEnumRef}}
{{^isEnumRef}}
{{#defaultValue}}
      /* Cross-cutting `default-applies-only-when-absent`: apply the default
         solely when the key is OMITTED; an explicit `null` is preserved as
         null per the JSON-Schema contract. */
      {{name}}: json.containsKey('{{baseName}}') ? (json['{{baseName}}'] != null ? {{{dataType}}}.fromJson(json['{{baseName}}'] as Map<String, dynamic>) : null) : {{{defaultValue}}},
{{/defaultValue}}
{{^defaultValue}}
      {{name}}: json['{{baseName}}'] != null ? {{{dataType}}}.fromJson(json['{{baseName}}'] as Map<String, dynamic>) : null,
{{/defaultValue}}
{{/isEnumRef}}
{{/isPrimitiveType}}
{{/isByteArray}}
{{/isMap}}
{{/isArray}}
{{/isEnum}}
{{/required}}
{{/vars}}
{{#isAdditionalPropertiesTrue}}
      additionalProperties: additionalProperties,
{{/isAdditionalPropertiesTrue}}
    );
  }

  /// Converts this [{{classname}}] to a JSON map.
  Map<String, dynamic> toJson() {
    final json = <String, dynamic>{};
{{#vars}}
{{#required}}
{{#isEnum}}
    json['{{baseName}}'] = {{name}}.toJson();
{{/isEnum}}
{{^isEnum}}
{{#isArray}}
{{#items}}
{{#isByteArray}}
    json['{{baseName}}'] = {{name}}.map((e) => base64Encode(e)).toList();
{{/isByteArray}}
{{^isByteArray}}
{{#isPrimitiveType}}
{{! A container of temporal/uuid/duration leaves holds Dart objects
    (DateTime/UuidValue/Duration) that jsonEncode cannot serialise, so each
    leaf must be projected to its wire form here — mirroring the scalar path
    above and the deserialize_arg leaf branches — instead of dumping the raw
    list. A `date` (format:date) leaf is already a String so it passes through
    via the plain toList(). }}
{{#isDateTime}}
    json['{{baseName}}'] = {{name}}.map((e) => formatDateTimeOffset(e)).toList();
{{/isDateTime}}
{{#isUuid}}
    json['{{baseName}}'] = {{name}}.map((e) => e.toString()).toList();
{{/isUuid}}
{{#vendorExtensions.isDuration}}
    json['{{baseName}}'] = {{name}}.map((e) => formatProtobufDuration(e)).toList();
{{/vendorExtensions.isDuration}}
{{^isDateTime}}{{^isUuid}}{{^vendorExtensions.isDuration}}
    json['{{baseName}}'] = {{name}}.toList();
{{/vendorExtensions.isDuration}}{{/isUuid}}{{/isDateTime}}
{{/isPrimitiveType}}
{{^isPrimitiveType}}
    json['{{baseName}}'] = {{name}}.map((e) => e.toJson()).toList();
{{/isPrimitiveType}}
{{/isByteArray}}
{{/items}}
{{/isArray}}
{{^isArray}}
{{#isByteArray}}
    json['{{baseName}}'] = base64Encode({{name}});
{{/isByteArray}}
{{^isByteArray}}
{{#isPrimitiveType}}
{{#isDateTime}}
    json['{{baseName}}'] = formatDateTimeOffset({{name}});
{{/isDateTime}}
{{#isDate}}
    json['{{baseName}}'] = {{name}};
{{/isDate}}
{{#isUuid}}
    json['{{baseName}}'] = {{name}}.toString();
{{/isUuid}}
{{#vendorExtensions.isDuration}}
    json['{{baseName}}'] = formatProtobufDuration({{name}});
{{/vendorExtensions.isDuration}}
{{^isDateTime}}{{^isDate}}{{^isUuid}}{{^vendorExtensions.isDuration}}
    json['{{baseName}}'] = {{name}};
{{/vendorExtensions.isDuration}}{{/isUuid}}{{/isDate}}{{/isDateTime}}
{{/isPrimitiveType}}
{{^isPrimitiveType}}
{{#isMap}}
{{#items}}
{{! A map whose VALUES are temporal/uuid/duration leaves holds Dart objects
    that jsonEncode cannot serialise, so each value is projected to its wire
    form here — mirroring the scalar/array paths and the deserialize_arg leaf
    branches. A `date` (format:date) value is already a String, and any other
    leaf passes through, via the plain assignment. }}
{{#isDateTime}}
    json['{{baseName}}'] = {{name}}.map((k, v) => MapEntry(k, formatDateTimeOffset(v)));
{{/isDateTime}}
{{#isUuid}}
    json['{{baseName}}'] = {{name}}.map((k, v) => MapEntry(k, v.toString()));
{{/isUuid}}
{{#vendorExtensions.isDuration}}
    json['{{baseName}}'] = {{name}}.map((k, v) => MapEntry(k, formatProtobufDuration(v)));
{{/vendorExtensions.isDuration}}
{{^isDateTime}}{{^isUuid}}{{^vendorExtensions.isDuration}}
    json['{{baseName}}'] = {{name}};
{{/vendorExtensions.isDuration}}{{/isUuid}}{{/isDateTime}}
{{/items}}
{{/isMap}}
{{^isMap}}
    json['{{baseName}}'] = {{name}}.toJson();
{{/isMap}}
{{/isPrimitiveType}}
{{/isByteArray}}
{{/isArray}}
{{/isEnum}}
{{/required}}
{{^required}}
    if ({{name}} != null) {
{{#isEnum}}
      json['{{baseName}}'] = {{name}}?.toJson();
{{/isEnum}}
{{^isEnum}}
{{#isArray}}
{{#items}}
{{#isByteArray}}
      json['{{baseName}}'] = {{name}}?.map((e) => base64Encode(e)).toList();
{{/isByteArray}}
{{^isByteArray}}
{{#isPrimitiveType}}
{{! Optional counterpart of the required array branch: a container of
    temporal/uuid/duration leaves holds Dart objects jsonEncode cannot encode,
    so each leaf is projected to its wire form. A `date` leaf is already a
    String and passes through the plain toList(). }}
{{#isDateTime}}
      json['{{baseName}}'] = {{name}}?.map((e) => formatDateTimeOffset(e)).toList();
{{/isDateTime}}
{{#isUuid}}
      json['{{baseName}}'] = {{name}}?.map((e) => e.toString()).toList();
{{/isUuid}}
{{#vendorExtensions.isDuration}}
      json['{{baseName}}'] = {{name}}?.map((e) => formatProtobufDuration(e)).toList();
{{/vendorExtensions.isDuration}}
{{^isDateTime}}{{^isUuid}}{{^vendorExtensions.isDuration}}
      json['{{baseName}}'] = {{name}}?.toList();
{{/vendorExtensions.isDuration}}{{/isUuid}}{{/isDateTime}}
{{/isPrimitiveType}}
{{^isPrimitiveType}}
      json['{{baseName}}'] = {{name}}?.map((e) => e.toJson()).toList();
{{/isPrimitiveType}}
{{/isByteArray}}
{{/items}}
{{/isArray}}
{{^isArray}}
{{#isByteArray}}
      json['{{baseName}}'] = base64Encode({{name}}!);
{{/isByteArray}}
{{^isByteArray}}
{{#isPrimitiveType}}
{{#isDateTime}}
      json['{{baseName}}'] = formatDateTimeOffset({{name}}!);
{{/isDateTime}}
{{#isDate}}
      json['{{baseName}}'] = {{name}};
{{/isDate}}
{{#isUuid}}
      json['{{baseName}}'] = {{name}}?.toString();
{{/isUuid}}
{{#vendorExtensions.isDuration}}
      json['{{baseName}}'] = formatProtobufDuration({{name}}!);
{{/vendorExtensions.isDuration}}
{{^isDateTime}}{{^isDate}}{{^isUuid}}{{^vendorExtensions.isDuration}}
      json['{{baseName}}'] = {{name}};
{{/vendorExtensions.isDuration}}{{/isUuid}}{{/isDate}}{{/isDateTime}}
{{/isPrimitiveType}}
{{^isPrimitiveType}}
{{#isMap}}
{{#items}}
{{! Optional counterpart of the required map branch: a map whose VALUES are
    temporal/uuid/duration leaves holds Dart objects jsonEncode cannot encode,
    so each value is projected to its wire form. A `date` value is already a
    String, and any other leaf passes through, via the plain assignment. }}
{{#isDateTime}}
      json['{{baseName}}'] = {{name}}?.map((k, v) => MapEntry(k, formatDateTimeOffset(v)));
{{/isDateTime}}
{{#isUuid}}
      json['{{baseName}}'] = {{name}}?.map((k, v) => MapEntry(k, v.toString()));
{{/isUuid}}
{{#vendorExtensions.isDuration}}
      json['{{baseName}}'] = {{name}}?.map((k, v) => MapEntry(k, formatProtobufDuration(v)));
{{/vendorExtensions.isDuration}}
{{^isDateTime}}{{^isUuid}}{{^vendorExtensions.isDuration}}
      json['{{baseName}}'] = {{name}};
{{/vendorExtensions.isDuration}}{{/isUuid}}{{/isDateTime}}
{{/items}}
{{/isMap}}
{{^isMap}}
      json['{{baseName}}'] = {{name}}?.toJson();
{{/isMap}}
{{/isPrimitiveType}}
{{/isByteArray}}
{{/isArray}}
{{/isEnum}}
    }
{{/required}}
{{/vars}}
{{#isAdditionalPropertiesTrue}}
    json.addAll(additionalProperties);
{{/isAdditionalPropertiesTrue}}
    return json;
  }

  /// Value-equality based on all declared fields{{#isAdditionalPropertiesTrue}} (including the
  /// additionalProperties map){{/isAdditionalPropertiesTrue}}. List, Map and Uint8List
  /// fields are compared element-wise via `DeepCollectionEquality` (Dart's
  /// built-in `==` on those is identity), so two instances decoded from
  /// identical JSON are equal and usable as Set/Map keys.
  @override
  bool operator ==(Object other) {
    if (identical(this, other)) return true;
    return other is {{classname}}{{#vars}} &&
        {{#isArray}}const DeepCollectionEquality().equals({{name}}, other.{{name}}){{/isArray}}{{^isArray}}{{#isMap}}const DeepCollectionEquality().equals({{name}}, other.{{name}}){{/isMap}}{{^isMap}}{{#isByteArray}}const DeepCollectionEquality().equals({{name}}, other.{{name}}){{/isByteArray}}{{^isByteArray}}{{name}} == other.{{name}}{{/isByteArray}}{{/isMap}}{{/isArray}}{{/vars}}{{#isAdditionalPropertiesTrue}} &&
        const DeepCollectionEquality().equals(additionalProperties, other.additionalProperties){{/isAdditionalPropertiesTrue}};
  }

  /// hashCode emits Object.hashAll which accepts an arbitrary-length
  /// Iterable (Object.hash requires 2+ positional args, so it can't
  /// represent the 0-var or 1-var cases without special-casing). Collection
  /// and byte fields are hashed structurally via `DeepCollectionEquality.hash`
  /// so equal instances hash equally.
  @override
  int get hashCode => Object.hashAll([{{#vars}}{{#isArray}}const DeepCollectionEquality().hash({{name}}){{/isArray}}{{^isArray}}{{#isMap}}const DeepCollectionEquality().hash({{name}}){{/isMap}}{{^isMap}}{{#isByteArray}}const DeepCollectionEquality().hash({{name}}){{/isByteArray}}{{^isByteArray}}{{name}}{{/isByteArray}}{{/isMap}}{{/isArray}}{{^-last}}, {{/-last}}{{/vars}}{{#isAdditionalPropertiesTrue}}{{#hasVars}}, {{/hasVars}}const DeepCollectionEquality().hash(additionalProperties){{/isAdditionalPropertiesTrue}}]);
}
{{/isPrimitiveAnyOf}}
{{/isPrimitiveOneOf}}
{{#isPrimitiveOneOf}}
{{#description}}
/// {{classname}} {{#lambda.unescapeDocComment}}{{{.}}}{{/lambda.unescapeDocComment}}
{{/description}}
{{^description}}
/// {{classname}} is a union type (oneOf) of primitive/array variants.
{{/description}}
{{#isDeprecated}}
@Deprecated('This schema is deprecated.')
{{/isDeprecated}}
class {{classname}} {
  /// The decoded payload. For a `string`/`byte` variant this is a
  /// [Uint8List]; for an array variant a [List]; for an object variant the
  /// generated model instance. Never null once constructed from the wire.
  final Object? _value;

  const {{classname}}._(this._value);

  /// Wraps an already-decoded value. The value must match one of the
  /// declared oneOf variants; it is re-encoded verbatim by [toJson].
  const {{classname}}.value(Object? value) : _value = value;

  /// Returns the underlying value of the union type.
  Object? get value => _value;

  /// Converts this union type back to its JSON wire value.
  Object? toJson() => _encodeValue(_value);

  /// Creates a [{{classname}}] from a decoded JSON value (a base64 [String]
  /// for a `byte` variant, a [List] for an array variant, or a
  /// `Map<String, dynamic>` for an object variant).
  factory {{classname}}.fromJson(Object? json) {
{{#composedSchemas}}
{{#oneOf}}
    try {
      return {{classname}}._(
{{#isByteArray}}
        base64Decode(json as String),
{{/isByteArray}}
{{^isByteArray}}
{{#isArray}}
{{#items}}
{{#isByteArray}}
        (json as List).map((e) => base64Decode(e as String)).{{#uniqueItems}}toSet(){{/uniqueItems}}{{^uniqueItems}}toList(){{/uniqueItems}},
{{/isByteArray}}
{{^isByteArray}}
{{#isPrimitiveType}}
        (json as List).map((e) => e as {{{dataType}}}).{{#uniqueItems}}toSet(){{/uniqueItems}}{{^uniqueItems}}toList(){{/uniqueItems}},
{{/isPrimitiveType}}
{{^isPrimitiveType}}
        (json as List).map((e) => {{{dataType}}}.fromJson(e as Map<String, dynamic>)).{{#uniqueItems}}toSet(){{/uniqueItems}}{{^uniqueItems}}toList(){{/uniqueItems}},
{{/isPrimitiveType}}
{{/isByteArray}}
{{/items}}
{{/isArray}}
{{^isArray}}
{{#isPrimitiveType}}
        json as {{{dataType}}},
{{/isPrimitiveType}}
{{^isPrimitiveType}}
        {{{dataType}}}.fromJson(json as Map<String, dynamic>),
{{/isPrimitiveType}}
{{/isArray}}
{{/isByteArray}}
      );
    } catch (_) {}
{{/oneOf}}
{{/composedSchemas}}
    /* Cross-cutting `oneof-nondiscriminator-no-match-silent`: no variant
       matched the wire shape. Throw a SerializationError rather than
       returning a silently-empty union that drops the payload. */
    throw SerializationError('Data does not match any oneOf schemas for {{classname}}');
  }

  /// Re-encodes a decoded value to its JSON wire form. Byte values are
  /// base64-encoded; generated models delegate to their own toJson; plain
  /// JSON primitives/maps pass through unchanged.
  static Object? _encodeValue(Object? v) {
    if (v == null) return null;
    if (v is Uint8List) return base64Encode(v);
    if (v is List) return v.map(_encodeValue).toList();
    if (v is Map) return v;
    try {
      return (v as dynamic).toJson();
    } catch (_) {
      return v;
    }
  }

  /// Value-equality on the decoded payload. The wrapped value may be a
  /// [Uint8List] (byte variant) or a [List] (array variant), whose built-in
  /// `==` is identity — so two unions decoded from identical wire bytes would
  /// otherwise compare unequal. `DeepCollectionEquality` compares those
  /// element-wise (and falls back to `==` for scalar/object variants), so
  /// equal payloads compare equal and hash equally.
  @override
  bool operator ==(Object other) {
    if (identical(this, other)) return true;
    return other is {{classname}} &&
        const DeepCollectionEquality().equals(_value, other._value);
  }

  @override
  int get hashCode => const DeepCollectionEquality().hash(_value);
}
{{/isPrimitiveOneOf}}
{{#isPrimitiveAnyOf}}
{{#description}}
/// {{classname}} {{#lambda.unescapeDocComment}}{{{.}}}{{/lambda.unescapeDocComment}}
{{/description}}
{{^description}}
/// {{classname}} is a union type (anyOf) of primitive/array variants.
{{/description}}
{{#isDeprecated}}
@Deprecated('This schema is deprecated.')
{{/isDeprecated}}
class {{classname}} {
  /// The decoded payload. For a `string`/`byte` variant this is a
  /// [Uint8List]; for an array variant a [List]; for an object variant the
  /// generated model instance. Never null once constructed from the wire.
  final Object? _value;

  const {{classname}}._(this._value);

  /// Wraps an already-decoded value. The value must match one of the
  /// declared anyOf variants; it is re-encoded verbatim by [toJson].
  const {{classname}}.value(Object? value) : _value = value;

  /// Returns the underlying value of the union type.
  Object? get value => _value;

  /// Converts this union type back to its JSON wire value.
  Object? toJson() => _encodeValue(_value);

  /// Creates a [{{classname}}] from a decoded JSON value (a base64 [String]
  /// for a `byte` variant, a [List] for an array variant, or a
  /// `Map<String, dynamic>` for an object variant).
  factory {{classname}}.fromJson(Object? json) {
{{#composedSchemas}}
{{#anyOf}}
    try {
      return {{classname}}._(
{{#isByteArray}}
        base64Decode(json as String),
{{/isByteArray}}
{{^isByteArray}}
{{#isArray}}
{{#items}}
{{#isByteArray}}
        (json as List).map((e) => base64Decode(e as String)).{{#uniqueItems}}toSet(){{/uniqueItems}}{{^uniqueItems}}toList(){{/uniqueItems}},
{{/isByteArray}}
{{^isByteArray}}
{{#isPrimitiveType}}
        (json as List).map((e) => e as {{{dataType}}}).{{#uniqueItems}}toSet(){{/uniqueItems}}{{^uniqueItems}}toList(){{/uniqueItems}},
{{/isPrimitiveType}}
{{^isPrimitiveType}}
        (json as List).map((e) => {{{dataType}}}.fromJson(e as Map<String, dynamic>)).{{#uniqueItems}}toSet(){{/uniqueItems}}{{^uniqueItems}}toList(){{/uniqueItems}},
{{/isPrimitiveType}}
{{/isByteArray}}
{{/items}}
{{/isArray}}
{{^isArray}}
{{#isPrimitiveType}}
        json as {{{dataType}}},
{{/isPrimitiveType}}
{{^isPrimitiveType}}
        {{{dataType}}}.fromJson(json as Map<String, dynamic>),
{{/isPrimitiveType}}
{{/isArray}}
{{/isByteArray}}
      );
    } catch (_) {}
{{/anyOf}}
{{/composedSchemas}}
    /* Cross-cutting `oneof-nondiscriminator-no-match-silent`: no variant
       matched the wire shape — surface a SerializationError rather than
       a silently-empty union that drops the payload. */
    throw SerializationError('Data does not match any anyOf schemas for {{classname}}');
  }

  /// Re-encodes a decoded value to its JSON wire form. Byte values are
  /// base64-encoded; generated models delegate to their own toJson; plain
  /// JSON primitives/maps pass through unchanged.
  static Object? _encodeValue(Object? v) {
    if (v == null) return null;
    if (v is Uint8List) return base64Encode(v);
    if (v is List) return v.map(_encodeValue).toList();
    if (v is Map) return v;
    try {
      return (v as dynamic).toJson();
    } catch (_) {
      return v;
    }
  }

  /// Value-equality on the decoded payload. The wrapped value may be a
  /// [Uint8List] (byte variant) or a [List] (array variant), whose built-in
  /// `==` is identity — so two unions decoded from identical wire bytes would
  /// otherwise compare unequal. `DeepCollectionEquality` compares those
  /// element-wise (and falls back to `==` for scalar/object variants), so
  /// equal payloads compare equal and hash equally.
  @override
  bool operator ==(Object other) {
    if (identical(this, other)) return true;
    return other is {{classname}} &&
        const DeepCollectionEquality().equals(_value, other._value);
  }

  @override
  int get hashCode => const DeepCollectionEquality().hash(_value);
}
{{/isPrimitiveAnyOf}}
{{/anyOf}}
{{/oneOf}}
{{#oneOf}}
{{#-first}}
{{#description}}
/// {{classname}} {{#lambda.unescapeDocComment}}{{{.}}}{{/lambda.unescapeDocComment}}
{{/description}}
{{^description}}
/// {{classname}} is a union type (oneOf).
{{/description}}
{{#isDeprecated}}
@Deprecated('This schema is deprecated.')
{{/isDeprecated}}
class {{classname}} {
  final Object? _value;

  const {{classname}}._(this._value);

  /// Returns the underlying value of the union type.
  Object? get value => _value;

  /// Converts this union type to a JSON value.
  Object? toJson() {
    final v = _value;
    if (v == null) return null;
    if (v is Map) return v;
    try {
      return (v as dynamic).toJson();
    } catch (_) {
      return v;
    }
  }

  /// Creates a [{{classname}}] from a JSON value.
  factory {{classname}}.fromJson(Map<String, dynamic> json) {
{{#discriminator}}
    final disc = json['{{propertyBaseName}}'];
    switch (disc) {
{{#mappedModels}}
      case '{{{mappingName}}}':
        return {{classname}}._({{{modelName}}}.fromJson(json));
{{/mappedModels}}
      default:
        /* 4.7 — discriminator value not in the listed mappings.
           Throw a SerializationError instead of falling through, so
           callers can distinguish wire-shape failures from generic
           argument errors. */
        throw SerializationError(
            'Unknown discriminator value for {{classname}}: $disc');
    }
{{/discriminator}}
{{^discriminator}}
{{#oneOf}}
    try {
      return {{classname}}._({{{.}}}.fromJson(json));
    } catch (_) {}
{{/oneOf}}
    /* Cross-cutting `oneof-nondiscriminator-no-match-silent`: no variant
       matched the wire shape. Throw a SerializationError (the same family
       as the discriminator no-match above) so the caller sees a wire-shape
       failure instead of a silently-empty union. */
    throw SerializationError('Data does not match any oneOf schemas for {{classname}}');
{{/discriminator}}
  }
}
{{/-first}}
{{/oneOf}}
{{#anyOf}}
{{#-first}}
{{#description}}
/// {{classname}} {{#lambda.unescapeDocComment}}{{{.}}}{{/lambda.unescapeDocComment}}
{{/description}}
{{^description}}
/// {{classname}} is a union type (anyOf).
{{/description}}
{{#isDeprecated}}
@Deprecated('This schema is deprecated.')
{{/isDeprecated}}
class {{classname}} {
  /* anyOf is NOT exclusive: a wire payload may satisfy several variants at
     once (e.g. a body carrying both Medication and Surgery fields). Retaining
     only the first match would silently drop the co-satisfied variants' data
     and lose it on re-encode. We therefore keep EVERY variant that decodes
     successfully and merge their field-sets on toJson, so the round-trip is
     lossless. */
  final List<Object?> _values;

  const {{classname}}._(this._values);

  /// Returns the first decoded variant, for back-compat with single-variant
  /// callers. Use [values] or [valueOf] to reach every retained variant.
  Object? get value => _values.isEmpty ? null : _values.first;

  /// Returns every variant that successfully decoded from the wire payload.
  /// For a co-satisfied anyOf this holds more than one element.
  List<Object?> get values => List.unmodifiable(_values);

  /// Returns the retained variant of type [T], or null if no decoded variant
  /// is a [T]. Lets callers reach a specific variant in a multi-match union.
  T? valueOf<T>() {
    for (final v in _values) {
      if (v is T) return v;
    }
    return null;
  }

  /// Converts this union type to a JSON value, merging the field-sets of all
  /// retained variants so co-satisfied data round-trips without loss. A lone
  /// non-map variant is emitted verbatim.
  Object? toJson() {
    if (_values.isEmpty) return null;
    if (_values.length == 1) {
      final v = _values.first;
      if (v == null) return null;
      if (v is Map) return v;
      try {
        return (v as dynamic).toJson();
      } catch (_) {
        return v;
      }
    }
    final merged = <String, dynamic>{};
    for (final v in _values) {
      if (v == null) continue;
      Object? encoded;
      if (v is Map) {
        encoded = v;
      } else {
        try {
          encoded = (v as dynamic).toJson();
        } catch (_) {
          encoded = v;
        }
      }
      if (encoded is Map) {
        encoded.forEach((key, dynamic val) => merged[key as String] = val);
      }
    }
    return merged;
  }

  /// Creates a [{{classname}}] from a JSON value, retaining every variant that
  /// decodes so a payload satisfying several variants keeps all their data.
  factory {{classname}}.fromJson(Map<String, dynamic> json) {
    final decoded = <Object?>[];
{{#anyOf}}
    try {
      decoded.add({{{.}}}.fromJson(json));
    } catch (_) {}
{{/anyOf}}
    if (decoded.isEmpty) {
      /* Cross-cutting `oneof-nondiscriminator-no-match-silent`: no variant
         matched the wire shape — surface a SerializationError rather than
         a silently-empty union. */
      throw SerializationError('Data does not match any anyOf schemas for {{classname}}');
    }
    return {{classname}}._(decoded);
  }
}
{{/-first}}
{{/anyOf}}
{{/isEnum}}
{{/model}}
{{/models}}
