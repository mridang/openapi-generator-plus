{{! Recursive phpstan/psalm type for a possibly-nested container. Rendered with a type node (a property or an items entry): an array node emits Type-of-elem, a map node emits Type-of-string-and-elem, and a leaf emits its bare dataType. Descending into the element at every level means a nested container such as an array of array of int is annotated as the fully-parameterized Ds Vector of Ds Vector of int rather than a one-level form that leaves the inner generic TValue unspecified. Kept on a single line with no trailing newline so it expands inline inside a var or param docblock line. }}{{#isArray}}{{{dataType}}}<{{#items}}{{>phpdoc_type}}{{/items}}>{{/isArray}}{{#isMap}}{{{dataType}}}<string, {{#items}}{{>phpdoc_type}}{{/items}}>{{/isMap}}{{^isArray}}{{^isMap}}{{{dataType}}}{{/isMap}}{{/isArray}}