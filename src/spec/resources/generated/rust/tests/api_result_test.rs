// Swagger Petstore - OpenAPI 3.0
// A simplified Pet Store API for integration testing.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

use std::collections::HashMap;

use petstore::ApiResult;

fn sample_headers() -> HashMap<String, String> {
    let mut headers = HashMap::new();
    headers.insert("Content-Type".to_string(), "application/json".to_string());
    headers
}

#[test]
fn test_exposes_status_code() {
    let result: ApiResult<String> =
        ApiResult::new(200, Some("ok".to_string()), "ok".to_string(), HashMap::new());
    assert_eq!(result.status_code(), 200);
}

#[test]
fn test_exposes_data() {
    let result: ApiResult<String> = ApiResult::new(
        200,
        Some("payload".to_string()),
        "payload".to_string(),
        HashMap::new(),
    );
    assert_eq!(result.data().map(|s| s.as_str()), Some("payload"));
}

#[test]
fn test_data_is_none_for_no_content() {
    let result: ApiResult<String> =
        ApiResult::new(204, None, String::new(), HashMap::new());
    assert!(result.data().is_none());
}

#[test]
fn test_into_data_consumes_result() {
    let result: ApiResult<String> = ApiResult::new(
        200,
        Some("payload".to_string()),
        "payload".to_string(),
        HashMap::new(),
    );
    assert_eq!(result.into_data(), Some("payload".to_string()));
}

#[test]
fn test_exposes_raw_body() {
    let result: ApiResult<String> = ApiResult::new(
        200,
        Some("ok".to_string()),
        "{\"value\":\"ok\"}".to_string(),
        HashMap::new(),
    );
    assert_eq!(result.raw_body(), "{\"value\":\"ok\"}");
}

#[test]
fn test_exposes_headers() {
    let result: ApiResult<String> = ApiResult::new(
        200,
        Some("ok".to_string()),
        "ok".to_string(),
        sample_headers(),
    );
    assert_eq!(
        result.headers().get("Content-Type").map(|s| s.as_str()),
        Some("application/json")
    );
}
