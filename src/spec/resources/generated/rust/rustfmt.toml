# Default rustfmt configuration.
#
# Generated code is formatted by `cargo fmt` during generation, so the output is
# rustfmt-clean by construction. No custom options are set: the project uses
# stock rustfmt defaults so callers get the same formatting the wider Rust
# ecosystem expects.
