# ruff: noqa
# mypy: ignore-errors
# Swagger Petstore - OpenAPI 3.0
# A simplified Pet Store API for integration testing.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

import re
import warnings
from pydantic import (
    BaseModel,
    ConfigDict,
    Field,
    field_validator,
    model_serializer,
    model_validator,
)
from pydantic import StrictBool, StrictFloat, StrictInt, StrictStr
from typing import Any, Callable, ClassVar, Dict, List, Optional, Set, Union
from typing_extensions import Self
from enum import Enum


class StockItem(BaseModel):
    """
    StockItem
    """

    priority: Priority = Field(alias="priority")
    availability: Optional[Availability] = Field(default=None, alias="availability")
    matrix: Optional[List[List[StrictInt]]] = Field(
        default=None,
        alias="matrix",
        description="Quantity grid as an array of integer arrays",
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from petstore_client.models.availability import Availability
from petstore_client.models.priority import Priority

StockItem.model_rebuild(raise_errors=False)
