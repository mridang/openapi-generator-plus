# Swagger Petstore - OpenAPI 3.0
# A simplified Pet Store API for integration testing.
#
# The version of the OpenAPI document: 1.0.0
#
# Generated by: https://openapi-generator.tech

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field
from typing import Dict, List, Optional


class BinaryVault(BaseModel):
    """
    BinaryVault
    """

    seal: Optional[Base64Bytes] = Field(
        default=None,
        alias="seal",
        description="A bare byte array; equality compares its contents.",
    )
    shards: Optional[List[Base64Bytes]] = Field(
        default=None,
        alias="shards",
        description="A list of byte arrays; equality compares element contents.",
    )
    labels: Optional[Dict[str, Base64Bytes]] = Field(
        default=None,
        alias="labels",
        description="A map of byte arrays; equality compares each value's contents.",
    )

    # Strict primitives (Item 8 — StrictInt/StrictStr/...) carry the
    # per-field strictness, so the model-wide ConfigDict no longer needs
    # strict=True. populate_by_name keeps both alias and snake_case
    # field-name kwargs working in constructors.
    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


from pydantic import Base64Bytes

BinaryVault.model_rebuild(raise_errors=False)
