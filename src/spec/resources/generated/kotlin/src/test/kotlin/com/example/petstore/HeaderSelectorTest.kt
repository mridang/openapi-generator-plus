// Swagger Petstore - OpenAPI 3.0
// A simplified Pet Store API for integration testing.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech


package com.example.petstore

import org.junit.jupiter.api.BeforeEach
import org.junit.jupiter.api.DisplayName
import org.junit.jupiter.api.Nested
import org.junit.jupiter.api.Test
import org.junit.jupiter.api.Assertions.*

class HeaderSelectorTest {

    private lateinit var selector: HeaderSelector

    @BeforeEach
    fun setUp() {
        selector = HeaderSelector()
    }

    @Nested
    @DisplayName("isJsonMime")
    inner class IsJsonMimeTests {

        @Test
        @DisplayName("returns true for application/json")
        fun returnsTrueForApplicationJson() {
            assertTrue(selector.isJsonMime("application/json"))
        }

        @Test
        @DisplayName("returns true for application/json with charset")
        fun returnsTrueForApplicationJsonWithCharset() {
            assertTrue(selector.isJsonMime("application/json; charset=UTF-8"))
        }

        @Test
        @DisplayName("returns true for APPLICATION/JSON (case insensitive)")
        fun returnsTrueForUppercaseApplicationJson() {
            assertTrue(selector.isJsonMime("APPLICATION/JSON"))
        }

        @Test
        @DisplayName("returns true for vendor JSON types")
        fun returnsTrueForVendorJsonTypes() {
            assertTrue(selector.isJsonMime("application/vnd.api+json"))
            assertTrue(selector.isJsonMime("application/vnd.company+json"))
            assertTrue(selector.isJsonMime("application/hal+json"))
        }

        @Test
        @DisplayName("returns false for text/html")
        fun returnsFalseForTextHtml() {
            assertFalse(selector.isJsonMime("text/html"))
        }

        @Test
        @DisplayName("returns false for application/xml")
        fun returnsFalseForApplicationXml() {
            assertFalse(selector.isJsonMime("application/xml"))
        }

        @Test
        @DisplayName("returns false for null")
        fun returnsFalseForNull() {
            assertFalse(selector.isJsonMime(null))
        }

        @Test
        @DisplayName("returns false for empty string")
        fun returnsFalseForEmptyString() {
            assertFalse(selector.isJsonMime(""))
        }

        @Test
        @DisplayName("returns false for text/plain")
        fun returnsFalseForTextPlain() {
            assertFalse(selector.isJsonMime("text/plain"))
        }

        @Test
        @DisplayName("returns false for application/octet-stream")
        fun returnsFalseForOctetStream() {
            assertFalse(selector.isJsonMime("application/octet-stream"))
        }
    }

    @Nested
    @DisplayName("selectHeaders")
    inner class SelectHeadersTests {

        @Test
        @DisplayName("sets Accept header for single accept")
        fun setsAcceptHeaderForSingleAccept() {
            val headers = selector.selectHeaders(
                arrayOf("application/json"),
                "application/json",
                false
            )
            assertEquals("application/json", headers["Accept"])
        }

        @Test
        @DisplayName("joins multiple non-JSON accepts with comma")
        fun joinsMultipleNonJsonAccepts() {
            val headers = selector.selectHeaders(
                arrayOf("text/html", "text/plain"),
                "application/json",
                false
            )
            assertEquals("text/html,text/plain", headers["Accept"])
        }

        @Test
        @DisplayName("does not set Accept header for empty accepts")
        fun doesNotSetAcceptForEmptyAccepts() {
            val headers = selector.selectHeaders(
                arrayOf(),
                "application/json",
                false
            )
            assertNull(headers["Accept"])
        }

        @Test
        @DisplayName("does not set Accept header for null accepts")
        fun doesNotSetAcceptForNullAccepts() {
            val headers = selector.selectHeaders(
                null,
                "application/json",
                false
            )
            assertNull(headers["Accept"])
        }

        @Test
        @DisplayName("multipart omits Content-Type")
        fun multipartOmitsContentType() {
            val headers = selector.selectHeaders(
                arrayOf("application/json"),
                "application/json",
                true
            )
            assertNull(headers["Content-Type"])
        }

        @Test
        @DisplayName("sets Content-Type when not multipart")
        fun setsContentTypeWhenNotMultipart() {
            val headers = selector.selectHeaders(
                arrayOf("application/json"),
                "application/json",
                false
            )
            assertEquals("application/json", headers["Content-Type"])
        }

        @Test
        @DisplayName("defaults Content-Type to application/json when empty")
        fun defaultsContentTypeToJsonWhenEmpty() {
            val headers = selector.selectHeaders(
                arrayOf("application/json"),
                "",
                false
            )
            assertEquals("application/json", headers["Content-Type"])
        }

        @Test
        @DisplayName("defaults Content-Type to application/json when null")
        fun defaultsContentTypeToJsonWhenNull() {
            val headers = selector.selectHeaders(
                arrayOf("application/json"),
                null,
                false
            )
            assertEquals("application/json", headers["Content-Type"])
        }
    }

    @Nested
    @DisplayName("quality weighting")
    inner class QualityWeighting {

        @Test
        @DisplayName("prioritizes application/json with quality weight")
        fun prioritizesApplicationJsonWithQualityWeight() {
            val headers = selector.selectHeaders(
                arrayOf("text/html", "application/json"),
                "application/json",
                false
            )
            val accept = headers["Accept"]
            assertNotNull(accept)
            assertTrue(accept!!.startsWith("application/json"))
            assertTrue(accept.contains("text/html"))
        }

        @Test
        @DisplayName("application/json has no quality suffix at weight 1000")
        fun applicationJsonHasNoQualitySuffix() {
            val headers = selector.selectHeaders(
                arrayOf("application/json", "text/html"),
                "application/json",
                false
            )
            val accept = headers["Accept"]
            assertNotNull(accept)
            assertTrue(
                accept!!.startsWith("application/json,") || accept == "application/json"
            )
        }

        @Test
        @DisplayName("non-JSON types get lower quality weight")
        fun nonJsonTypesGetLowerWeight() {
            val headers = selector.selectHeaders(
                arrayOf("application/json", "text/html"),
                "application/json",
                false
            )
            val accept = headers["Accept"]
            assertNotNull(accept)
            assertTrue(accept!!.contains("text/html;q=0.9") || accept.contains("text/html;q=0."))
        }

        @Test
        @DisplayName("removes trailing zeros from quality weight")
        fun removesTrailingZeros() {
            val headers = selector.selectHeaders(
                arrayOf("application/json", "text/html"),
                "application/json",
                false
            )
            val accept = headers["Accept"]
            assertNotNull(accept)
            assertFalse(accept!!.contains(";q=0.900"))
        }

        @Test
        @DisplayName("multiple JSON types ordered before non-JSON types")
        fun multipleJsonTypesOrderedBeforeNonJson() {
            val headers = selector.selectHeaders(
                arrayOf("text/html", "application/vnd.api+json", "application/json"),
                "application/json",
                false
            )
            val accept = headers["Accept"]
            assertNotNull(accept)
            val jsonIndex = accept!!.indexOf("application/json")
            val vendorIndex = accept.indexOf("application/vnd.api+json")
            val htmlIndex = accept.indexOf("text/html")
            assertTrue(jsonIndex < vendorIndex)
            assertTrue(vendorIndex < htmlIndex)
        }
    }

    @Nested
    @DisplayName("vendor JSON")
    inner class VendorJson {

        @Test
        @DisplayName("vendor JSON type is recognized as JSON")
        fun vendorJsonIsRecognized() {
            assertTrue(selector.isJsonMime("application/vnd.api+json"))
        }

        @Test
        @DisplayName("vendor JSON prioritized over non-JSON in accept header")
        fun vendorJsonPrioritizedOverNonJson() {
            val headers = selector.selectHeaders(
                arrayOf("text/plain", "application/vnd.api+json"),
                "application/json",
                false
            )
            val accept = headers["Accept"]
            assertNotNull(accept)
            val vendorIndex = accept!!.indexOf("application/vnd.api+json")
            val plainIndex = accept.indexOf("text/plain")
            assertTrue(vendorIndex < plainIndex)
        }
    }

    @Nested
    @DisplayName("getNextWeight")
    inner class GetNextWeightTests {

        @Test
        @DisplayName("standard weight sequence for <= 28 headers")
        fun standardWeightSequence() {
            assertEquals(900, selector.getNextWeight(1000, false))
            assertEquals(800, selector.getNextWeight(900, false))
            assertEquals(100, selector.getNextWeight(200, false))
            assertEquals(90, selector.getNextWeight(100, false))
            assertEquals(80, selector.getNextWeight(90, false))
        }

        @Test
        @DisplayName("1-by-1 decrement for > 28 headers")
        fun oneByOneDecrement() {
            assertEquals(999, selector.getNextWeight(1000, true))
            assertEquals(998, selector.getNextWeight(999, true))
        }

        @Test
        @DisplayName("returns 1 when weight is 1 or less")
        fun returnsOneWhenWeightIsOneOrLess() {
            assertEquals(1, selector.getNextWeight(1, false))
            assertEquals(1, selector.getNextWeight(0, false))
        }

        @Test
        @DisplayName("produces exactly 27 steps from 1000 to 1")
        fun producesExactly27Steps() {
            var weight = 1000
            var count = 0
            while (weight > 1) {
                weight = selector.getNextWeight(weight, false)
                count++
            }
            assertEquals(27, count)
        }
    }
}
