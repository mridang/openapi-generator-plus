// Swagger Petstore - OpenAPI 3.0
// A simplified Pet Store API for integration testing.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

// ignore_for_file: unused_import, no_leading_underscores_for_local_identifiers, unintended_html_in_doc_comment
import 'dart:convert';
import 'dart:typed_data';

import '../object_serializer.dart';

/// WetFood is a model class generated from the OpenAPI schema.
class WetFood {
  /// Example: `null`
  final String foodType;

  /// Example: `null`
  final int volumeMl;

  const WetFood({this.foodType = 'wet', required this.volumeMl});

  /// Creates a [WetFood] from a JSON map.
  factory WetFood.fromJson(Map<String, dynamic> json) {
    /* DIVERGENCE #10 — a REQUIRED, non-nullable field must be present and
       non-null on the wire. An absent key reads as null in Dart, so this
       single guard covers both the missing and explicitly-null cases. We
       raise the SDK's own SerializationError (not an incidental cast/TypeError)
       so callers see a uniform, catchable wire-shape failure. */
    if (json['foodType'] == null) {
      throw SerializationError("Missing required field 'foodType' for WetFood");
    }
    /* DIVERGENCE #10 — a REQUIRED, non-nullable field must be present and
       non-null on the wire. An absent key reads as null in Dart, so this
       single guard covers both the missing and explicitly-null cases. We
       raise the SDK's own SerializationError (not an incidental cast/TypeError)
       so callers see a uniform, catchable wire-shape failure. */
    if (json['volumeMl'] == null) {
      throw SerializationError("Missing required field 'volumeMl' for WetFood");
    }
    return WetFood(
      foodType: json['foodType'] as String,

      volumeMl: json['volumeMl'] as int,
    );
  }

  /// Converts this [WetFood] to a JSON map.
  Map<String, dynamic> toJson() {
    final json = <String, dynamic>{};

    json['foodType'] = foodType;

    json['volumeMl'] = volumeMl;

    return json;
  }

  /// Value-equality based on all declared fields. Nested List/Map fields are compared
  /// by reference — callers needing structural equality on those should
  /// use `package:collection`'s `DeepCollectionEquality`.
  @override
  bool operator ==(Object other) {
    if (identical(this, other)) return true;
    return other is WetFood &&
        foodType == other.foodType &&
        volumeMl == other.volumeMl;
  }

  /// hashCode emits Object.hashAll which accepts an arbitrary-length
  /// Iterable (Object.hash requires 2+ positional args, so it can't
  /// represent the 0-var or 1-var cases without special-casing).
  @override
  int get hashCode => Object.hashAll([foodType, volumeMl]);
}
