// Swagger Petstore - OpenAPI 3.0
// A simplified Pet Store API for integration testing.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import 'dart:convert';

/// Order is a model class generated from the OpenAPI schema.
class Order {
  /// Example: `10`
  final int? id;

  /// Example: `198772`
  final int? petId;

  /// Example: `7`
  final int? quantity;

  /// Example: `null`
  final DateTime? shipDate;

  /// Order Status
  /// Example: `approved`
  final String? status;

  /// Example: `null`
  final bool? complete;

  const Order({
    this.id,
    this.petId,
    this.quantity,
    this.shipDate,
    this.status = 'placed',
    this.complete,
  });

  /// Creates a [Order] from a JSON map.
  factory Order.fromJson(Map<String, dynamic> json) {
    return Order(

      id: json['id'] as int?,

      petId: json['petId'] as int?,

      quantity: json['quantity'] as int?,

      shipDate: json['shipDate'] != null ? DateTime.parse(json['shipDate'] as String) : null,

      status: json['status'] != null ? json['status'] as String : 'placed',

      complete: json['complete'] as bool?,

    );
  }

  /// Converts this [Order] to a JSON map.
  Map<String, dynamic> toJson() {
    final json = <String, dynamic>{};
    if (id != null) {

      json['id'] = id;

    }
    if (petId != null) {

      json['petId'] = petId;

    }
    if (quantity != null) {

      json['quantity'] = quantity;

    }
    if (shipDate != null) {
      json['shipDate'] = shipDate?.toUtc().toIso8601String().replaceFirst(RegExp(r'(\.\d+)?Z$'), '+00:00');

    }
    if (status != null) {

      json['status'] = status;

    }
    if (complete != null) {

      json['complete'] = complete;

    }
    return json;
  }

  /// Value-equality based on all declared fields. Nested List/Map fields are compared
  /// by reference — callers needing structural equality on those should
  /// use `package:collection`'s `DeepCollectionEquality`.
  @override
  bool operator ==(Object other) {
    if (identical(this, other)) return true;
    return other is Order &&
        id == other.id &&
        petId == other.petId &&
        quantity == other.quantity &&
        shipDate == other.shipDate &&
        status == other.status &&
        complete == other.complete;
  }

  /// hashCode emits Object.hashAll which accepts an arbitrary-length
  /// Iterable (Object.hash requires 2+ positional args, so it can't
  /// represent the 0-var or 1-var cases without special-casing).
  @override
  int get hashCode => Object.hashAll([id, petId, quantity, shipDate, status, complete]);
}
