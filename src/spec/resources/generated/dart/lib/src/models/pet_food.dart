// Swagger Petstore - OpenAPI 3.0
// A simplified Pet Store API for integration testing.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import 'dart:convert';
import 'dart:typed_data';

import '../object_serializer.dart';

import 'dry_food.dart';
import 'wet_food.dart';

/// PetFood Food for pets, discriminated by foodType
class PetFood {
  final Object? _value;

  const PetFood._(this._value);

  /// Returns the underlying value of the union type.
  Object? get value => _value;

  /// Converts this union type to a JSON value.
  Object? toJson() {
    final v = _value;
    if (v == null) return null;
    if (v is Map) return v;
    try {
      return (v as dynamic).toJson();
    } catch (_) {
      return v;
    }
  }

  /// Creates a [PetFood] from a JSON value.
  factory PetFood.fromJson(Map<String, dynamic> json) {
    final disc = json['foodType'];
    switch (disc) {
      case 'dry':
        return PetFood._(DryFood.fromJson(json));
      case 'wet':
        return PetFood._(WetFood.fromJson(json));
      default:
        /* 4.7 — discriminator value not in the listed mappings.
           Throw a SerializationError instead of falling through, so
           callers can distinguish wire-shape failures from generic
           argument errors. */
        throw SerializationError(
          'Unknown discriminator value for PetFood: $disc',
        );
    }
  }
}
