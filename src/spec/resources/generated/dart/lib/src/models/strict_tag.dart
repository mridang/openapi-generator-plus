// Swagger Petstore - OpenAPI 3.0
// A simplified Pet Store API for integration testing.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

/// StrictTag is a model class generated from the OpenAPI schema.
class StrictTag {
  const StrictTag({
    this.id,
    this.name,
  });

  /// Creates a [StrictTag] from a JSON map.
  factory StrictTag.fromJson(Map<String, dynamic> json) {
    // Gap AX.1 — OAS 3.1 / JSON Schema 2020-12 unevaluatedProperties:false.
    // Reject any JSON key not declared above.
    const declaredKeys = <String>{
      'id',
      'name',
    };
    for (final key in json.keys) {
      if (!declaredKeys.contains(key)) {
        throw ArgumentError(
          "Unknown property '$key' on StrictTag (unevaluatedProperties:false)",
        );
      }
    }
    return StrictTag(
      id: json['id'] as int?,
      name: json['name'] as String?,
    );
  }

  /// Example: `null`
  final int? id;

  /// Example: `null`
  final String? name;

  /// Converts this [StrictTag] to a JSON map.
  Map<String, dynamic> toJson() {
    final json = <String, dynamic>{};
    if (id != null) {
      json['id'] = id;
    }
    if (name != null) {
      json['name'] = name;
    }
    return json;
  }

  /// Value-equality based on all declared fields. Nested List/Map fields are compared
  /// by reference — callers needing structural equality on those should
  /// use `package:collection`'s `DeepCollectionEquality`.
  @override
  bool operator ==(Object other) {
    if (identical(this, other)) return true;
    return other is StrictTag && id == other.id && name == other.name;
  }

  /// hashCode emits Object.hashAll which accepts an arbitrary-length
  /// Iterable (Object.hash requires 2+ positional args, so it can't
  /// represent the 0-var or 1-var cases without special-casing).
  @override
  int get hashCode => Object.hashAll([id, name]);
}
