// Swagger Petstore - OpenAPI 3.0
// A simplified Pet Store API for integration testing.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

// ignore_for_file: unused_import, no_leading_underscores_for_local_identifiers, unintended_html_in_doc_comment
import 'dart:convert';
import 'dart:typed_data';

import 'package:collection/collection.dart';

import '../object_serializer.dart';

/// BinaryVault is a model class generated from the OpenAPI schema.
class BinaryVault {
  /// A bare byte array; equality compares its contents.
  final Uint8List? seal;

  /// A list of byte arrays; equality compares element contents.
  final List<Uint8List>? shards;

  /// A map of byte arrays; equality compares each value's contents.
  final Map<String, Uint8List>? labels;

  const BinaryVault({this.seal, this.shards, this.labels});

  /// Creates a [BinaryVault] from a JSON map.
  factory BinaryVault.fromJson(Map<String, dynamic> json) {
    return BinaryVault(
      seal: json['seal'] != null ? base64Decode(json['seal'] as String) : null,
      shards: json['shards'] != null
          ? deserializeArray(
              json['shards'] as List,
              (e) => base64Decode(e as String),
            )
          : null,
      labels: (json['labels'] as Map<String, dynamic>?)
          ?.cast<String, Uint8List>(),
    );
  }

  /// Converts this [BinaryVault] to a JSON map.
  Map<String, dynamic> toJson() {
    final json = <String, dynamic>{};
    if (seal != null) {
      json['seal'] = base64Encode(seal!);
    }
    if (shards != null) {
      json['shards'] = shards?.map((e) => base64Encode(e)).toList();
    }
    if (labels != null) {
      json['labels'] = labels;
    }
    return json;
  }

  /// Value-equality based on all declared fields. List, Map and Uint8List
  /// fields are compared element-wise via `DeepCollectionEquality` (Dart's
  /// built-in `==` on those is identity), so two instances decoded from
  /// identical JSON are equal and usable as Set/Map keys.
  @override
  bool operator ==(Object other) {
    if (identical(this, other)) return true;
    return other is BinaryVault &&
        const DeepCollectionEquality().equals(seal, other.seal) &&
        const DeepCollectionEquality().equals(shards, other.shards) &&
        const DeepCollectionEquality().equals(labels, other.labels);
  }

  /// hashCode emits Object.hashAll which accepts an arbitrary-length
  /// Iterable (Object.hash requires 2+ positional args, so it can't
  /// represent the 0-var or 1-var cases without special-casing). Collection
  /// and byte fields are hashed structurally via `DeepCollectionEquality.hash`
  /// so equal instances hash equally.
  @override
  int get hashCode => Object.hashAll([
    const DeepCollectionEquality().hash(seal),
    const DeepCollectionEquality().hash(shards),
    const DeepCollectionEquality().hash(labels),
  ]);
}
