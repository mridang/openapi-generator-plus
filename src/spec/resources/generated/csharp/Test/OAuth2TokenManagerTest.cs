// Swagger Petstore - OpenAPI 3.0
// A simplified Pet Store API for integration testing.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

using PetstoreClient;
using PetstoreClient.Auth.OAuth;
using Xunit;

namespace Test;

public class OAuth2TokenManagerTest
{
    private sealed class FakeApiClient : IApiClient
    {
        private readonly Queue<ApiResponse> _responses = new();

        public void Enqueue(string body, int statusCode = 200)
        {
            _responses.Enqueue(new ApiResponse(statusCode, body, new Dictionary<string, string>()));
        }

        public string? LastBody { get; private set; }
        public Uri? LastUrl { get; private set; }
        public bool LastNoRedirect { get; private set; }

        public Task<ApiResponse> SendRequestAsync(
            string method,
            Uri url,
            Dictionary<string, string> headers,
            object? body,
            bool noRedirect = false
        )
        {
            LastUrl = url;
            LastBody = body?.ToString();
            LastNoRedirect = noRedirect;
            return Task.FromResult(_responses.Dequeue());
        }
    }

    [Fact]
    public async Task ExtractsAccessTokenFromResponse()
    {
        var client = new FakeApiClient();
        client.Enqueue("{\"access_token\":\"tok123\",\"expires_in\":3600}");

        var manager = new OAuth2TokenManager();
        manager.SetApiClient(client);

        string token = await manager.GetAccessTokenAsync(
            new Uri("https://auth.example.com/token"),
            new Dictionary<string, string> { ["grant_type"] = "client_credentials" }
        );

        Assert.Equal("tok123", token);
    }

    [Fact]
    public async Task StoresRefreshToken()
    {
        var client = new FakeApiClient();
        client.Enqueue(
            "{\"access_token\":\"tok1\",\"refresh_token\":\"ref1\",\"expires_in\":3600}"
        );

        var manager = new OAuth2TokenManager();
        manager.SetApiClient(client);

        await manager.GetAccessTokenAsync(
            new Uri("https://auth.example.com/token"),
            new Dictionary<string, string> { ["grant_type"] = "authorization_code" }
        );

        Assert.Equal("ref1", manager.RefreshToken);
    }

    [Fact]
    public async Task ReturnsCachedTokenWhenNotExpired()
    {
        var client = new FakeApiClient();
        client.Enqueue("{\"access_token\":\"tok1\",\"expires_in\":3600}");

        var manager = new OAuth2TokenManager();
        manager.SetApiClient(client);

        var parameters = new Dictionary<string, string> { ["grant_type"] = "client_credentials" };
        var tokenUrl = new Uri("https://auth.example.com/token");

        string first = await manager.GetAccessTokenAsync(tokenUrl, parameters);
        string second = await manager.GetAccessTokenAsync(tokenUrl, parameters);

        Assert.Equal("tok1", first);
        Assert.Equal("tok1", second);
    }

    [Fact]
    public async Task RefetchesTokenWhenExpired()
    {
        var client = new FakeApiClient();
        // First token with 0 seconds expiry (will be treated as expired due to 30s buffer)
        client.Enqueue("{\"access_token\":\"tok1\",\"expires_in\":1}");
        client.Enqueue("{\"access_token\":\"tok2\",\"expires_in\":3600}");

        var manager = new OAuth2TokenManager();
        manager.SetApiClient(client);

        var parameters = new Dictionary<string, string> { ["grant_type"] = "client_credentials" };
        var tokenUrl = new Uri("https://auth.example.com/token");

        // First call fetches tok1, but it expires immediately (expires_in=1, minus 30s buffer)
        string first = await manager.GetAccessTokenAsync(tokenUrl, parameters);
        // Second call should refetch since token is expired
        string second = await manager.GetAccessTokenAsync(tokenUrl, parameters);

        Assert.Equal("tok1", first);
        Assert.Equal("tok2", second);
    }

    [Fact]
    public async Task SetAccessTokenBypassesEndpoint()
    {
        var manager = new OAuth2TokenManager();
        manager.SetAccessToken("manual-token");

        // No API client needed since token is set manually
        string token = await manager.GetAccessTokenAsync(
            new Uri("https://auth.example.com/token"),
            new Dictionary<string, string>()
        );

        Assert.Equal("manual-token", token);
    }

    [Fact]
    public async Task InvalidateAccessTokenForcesRefetch()
    {
        var client = new FakeApiClient();
        client.Enqueue("{\"access_token\":\"tok1\",\"expires_in\":3600}");
        client.Enqueue("{\"access_token\":\"tok2\",\"expires_in\":3600}");

        var manager = new OAuth2TokenManager();
        manager.SetApiClient(client);

        var parameters = new Dictionary<string, string> { ["grant_type"] = "client_credentials" };
        var tokenUrl = new Uri("https://auth.example.com/token");

        string first = await manager.GetAccessTokenAsync(tokenUrl, parameters);
        manager.InvalidateAccessToken();
        string second = await manager.GetAccessTokenAsync(tokenUrl, parameters);

        Assert.Equal("tok1", first);
        Assert.Equal("tok2", second);
    }

    [Fact]
    public async Task ThrowsWhenNoApiClientInjected()
    {
        var manager = new OAuth2TokenManager();

        await Assert.ThrowsAsync<InvalidOperationException>(() =>
            manager.GetAccessTokenAsync(
                new Uri("https://auth.example.com/token"),
                new Dictionary<string, string> { ["grant_type"] = "client_credentials" }
            )
        );
    }

    [Fact]
    public async Task ConcurrentCallersTriggerOnlyOneTokenRequest()
    {
        // Gap G: single-flight refresh. 10 concurrent callers immediately after
        // invalidation must result in exactly ONE HTTP call to the token endpoint.
        var client = new CountingDelayingApiClient(
            "{\"access_token\":\"tok-concurrent\",\"expires_in\":3600}"
        );

        var manager = new OAuth2TokenManager();
        manager.SetApiClient(client);

        var parameters = new Dictionary<string, string> { ["grant_type"] = "client_credentials" };
        var tokenUrl = new Uri("https://auth.example.com/token");

        Task<string>[] tasks = new Task<string>[10];
        for (int i = 0; i < tasks.Length; i++)
        {
            tasks[i] = manager.GetAccessTokenAsync(tokenUrl, parameters);
        }

        string[] tokens = await Task.WhenAll(tasks);

        Assert.Equal(1, client.CallCount);
        foreach (string t in tokens)
        {
            Assert.Equal("tok-concurrent", t);
        }
    }

    private sealed class CountingDelayingApiClient : IApiClient
    {
        private readonly string _body;
        private int _callCount;

        public CountingDelayingApiClient(string body)
        {
            _body = body;
        }

        public int CallCount => _callCount;

        public async Task<ApiResponse> SendRequestAsync(
            string method,
            Uri url,
            Dictionary<string, string> headers,
            object? body,
            bool noRedirect = false
        )
        {
            System.Threading.Interlocked.Increment(ref _callCount);
            // Hold the "in flight" request long enough that all concurrent
            // callers pile up behind the single-flight lock.
            await Task.Delay(50).ConfigureAwait(false);
            return new ApiResponse(200, _body, new Dictionary<string, string>());
        }
    }

    [Fact]
    public async Task ExpiresInShortLivedTokenDoesNotStorm()
    {
        // Gap CM: short-lived token (expires_in < 30s buffer) — a single
        // GetAccessTokenAsync call must produce exactly ONE network call.
        var client = new FakeApiClient();
        client.Enqueue("{\"access_token\":\"short\",\"expires_in\":10}");

        var manager = new OAuth2TokenManager();
        manager.SetApiClient(client);

        string token = await manager.GetAccessTokenAsync(
            new Uri("https://auth.example.com/token"),
            new Dictionary<string, string> { ["grant_type"] = "client_credentials" }
        );

        Assert.Equal("short", token);
    }

    [Fact]
    public async Task ExpiresInLongLivedTokenAppliesFullBuffer()
    {
        // Gap CM: long-lived token — full 30s buffer applied; second call within
        // the same instant must serve from cache.
        var client = new FakeApiClient();
        client.Enqueue("{\"access_token\":\"long\",\"expires_in\":3600}");

        var manager = new OAuth2TokenManager();
        manager.SetApiClient(client);

        var parameters = new Dictionary<string, string> { ["grant_type"] = "client_credentials" };
        var tokenUrl = new Uri("https://auth.example.com/token");

        string first = await manager.GetAccessTokenAsync(tokenUrl, parameters);
        string second = await manager.GetAccessTokenAsync(tokenUrl, parameters);

        Assert.Equal("long", first);
        Assert.Equal("long", second);
    }

    [Fact]
    public async Task ExpiresInExactlyBufferReturnsZeroBuffer()
    {
        // Gap CM: expires_in == 30 collapses expiry to now, forcing a refetch
        // on the next call.
        var client = new FakeApiClient();
        client.Enqueue("{\"access_token\":\"edge1\",\"expires_in\":30}");
        client.Enqueue("{\"access_token\":\"edge2\",\"expires_in\":30}");

        var manager = new OAuth2TokenManager();
        manager.SetApiClient(client);

        var parameters = new Dictionary<string, string> { ["grant_type"] = "client_credentials" };
        var tokenUrl = new Uri("https://auth.example.com/token");

        string first = await manager.GetAccessTokenAsync(tokenUrl, parameters);
        string second = await manager.GetAccessTokenAsync(tokenUrl, parameters);

        Assert.Equal("edge1", first);
        Assert.Equal("edge2", second);
    }

    [Fact]
    public async Task RefreshTokenEmptyStringPreservesExisting()
    {
        // Gap A3 (RFC 6749 §6): an empty refresh_token in a refresh response
        // MUST NOT clobber the cached refresh_token.
        var client = new FakeApiClient();
        client.Enqueue(
            "{\"access_token\":\"old_access\",\"refresh_token\":\"old_refresh\",\"expires_in\":1}"
        );
        client.Enqueue(
            "{\"access_token\":\"new_access\",\"expires_in\":3600,\"refresh_token\":\"\"}"
        );

        var manager = new OAuth2TokenManager();
        manager.SetApiClient(client);

        var parameters = new Dictionary<string, string> { ["grant_type"] = "authorization_code" };
        var tokenUrl = new Uri("https://auth.example.com/token");

        await manager.GetAccessTokenAsync(tokenUrl, parameters);
        Assert.Equal("old_refresh", manager.RefreshToken);

        // Second call: access token near-expiry, refresh fires with "old_refresh";
        // server replies with empty refresh_token which must not clobber the cache.
        await manager.GetAccessTokenAsync(tokenUrl, parameters);
        Assert.Equal("old_refresh", manager.RefreshToken);
    }

    [Fact]
    public async Task ExpiresInAsJsonStringIsAccepted()
    {
        // D1: RFC 6749 §5.1 — providers like Salesforce send expires_in as a
        // quoted string. The manager must accept it and cache the token; a
        // second call within the buffer window must serve from cache.
        var client = new FakeApiClient();
        client.Enqueue("{\"access_token\":\"str-tok\",\"expires_in\":\"3600\"}");

        var manager = new OAuth2TokenManager();
        manager.SetApiClient(client);

        var parameters = new Dictionary<string, string> { ["grant_type"] = "client_credentials" };
        var tokenUrl = new Uri("https://auth.example.com/token");

        string first = await manager.GetAccessTokenAsync(tokenUrl, parameters);
        string second = await manager.GetAccessTokenAsync(tokenUrl, parameters);

        Assert.Equal("str-tok", first);
        Assert.Equal("str-tok", second);
    }

    [Fact]
    public async Task ExpiresInAsFloatIsFloored()
    {
        // D1: some providers send expires_in as a JSON float (e.g. 3600.5).
        // The manager must floor it and cache the token.
        var client = new FakeApiClient();
        client.Enqueue("{\"access_token\":\"flt-tok\",\"expires_in\":3600.5}");

        var manager = new OAuth2TokenManager();
        manager.SetApiClient(client);

        var parameters = new Dictionary<string, string> { ["grant_type"] = "client_credentials" };
        var tokenUrl = new Uri("https://auth.example.com/token");

        string first = await manager.GetAccessTokenAsync(tokenUrl, parameters);
        string second = await manager.GetAccessTokenAsync(tokenUrl, parameters);

        Assert.Equal("flt-tok", first);
        Assert.Equal("flt-tok", second);
    }

    [Fact]
    public async Task ExpiresInNegativeSkipsCaching()
    {
        // D1: a negative expires_in (e.g. -1) must mark the token as
        // immediately stale so the very next call refetches.
        var client = new FakeApiClient();
        client.Enqueue("{\"access_token\":\"neg1\",\"expires_in\":-1}");
        client.Enqueue("{\"access_token\":\"neg2\",\"expires_in\":3600}");

        var manager = new OAuth2TokenManager();
        manager.SetApiClient(client);

        var parameters = new Dictionary<string, string> { ["grant_type"] = "client_credentials" };
        var tokenUrl = new Uri("https://auth.example.com/token");

        string first = await manager.GetAccessTokenAsync(tokenUrl, parameters);
        string second = await manager.GetAccessTokenAsync(tokenUrl, parameters);

        Assert.Equal("neg1", first);
        Assert.Equal("neg2", second);
    }

    [Fact]
    public async Task ThrowsWhenTokenRequestFails()
    {
        var client = new FakeApiClient();
        client.Enqueue("{\"error\":\"invalid_client\"}", statusCode: 401);

        var manager = new OAuth2TokenManager();
        manager.SetApiClient(client);

        await Assert.ThrowsAsync<OAuth2ServerError>(() =>
            manager.GetAccessTokenAsync(
                new Uri("https://auth.example.com/token"),
                new Dictionary<string, string> { ["grant_type"] = "client_credentials" }
            )
        );
    }

    [Fact]
    public async Task TokenResponseMissingAccessTokenThrowsTypedError()
    {
        var client = new FakeApiClient();
        client.Enqueue("{\"refresh_token\":\"x\"}");

        var manager = new OAuth2TokenManager();
        manager.SetApiClient(client);

        await Assert.ThrowsAsync<OAuth2TokenError>(() =>
            manager.GetAccessTokenAsync(
                new Uri("https://auth.example.com/token"),
                new Dictionary<string, string> { ["grant_type"] = "client_credentials" }
            )
        );
    }

    [Fact]
    public async Task TokenEndpointErrorResponseParsedToTypedError()
    {
        var client = new FakeApiClient();
        client.Enqueue(
            "{\"error\":\"invalid_grant\",\"error_description\":\"refresh token expired\","
                + "\"error_uri\":\"https://docs.example.com/errors/invalid_grant\"}",
            statusCode: 400
        );

        var manager = new OAuth2TokenManager();
        manager.SetApiClient(client);

        var ex = await Assert.ThrowsAsync<OAuth2ServerError>(() =>
            manager.GetAccessTokenAsync(
                new Uri("https://auth.example.com/token"),
                new Dictionary<string, string> { ["grant_type"] = "client_credentials" }
            )
        );
        Assert.Equal(400, ex.StatusCode);
        Assert.Equal("invalid_grant", ex.Code);
        Assert.Equal("refresh token expired", ex.Description);
        Assert.Equal("https://docs.example.com/errors/invalid_grant", ex.Uri);
    }

    // ---- 3.2: token POSTs must refuse ALL 3xx redirects ----

    [Fact]
    public async Task TokenRequestSetsNoRedirect()
    {
        // The token manager must pass noRedirect=true so the transport
        // does not silently follow a malicious 307 from the token URL.
        var client = new FakeApiClient();
        client.Enqueue("{\"access_token\":\"tok\",\"expires_in\":3600}");

        var manager = new OAuth2TokenManager();
        manager.SetApiClient(client);

        await manager.GetAccessTokenAsync(
            new Uri("https://auth.example.com/token"),
            new Dictionary<string, string> { ["grant_type"] = "client_credentials" }
        );

        Assert.True(client.LastNoRedirect);
    }

    // RFC 6749 §3.2 forbids redirects at the token endpoint; the manager must
    // reject the whole 300-399 range (302 and 307 both exercised here, plus
    // 301/303/308) rather than only the body-preserving 307/308.
    [Theory]
    [InlineData(301)]
    [InlineData(302)]
    [InlineData(303)]
    [InlineData(307)]
    [InlineData(308)]
    public async Task RefusesRedirectFromTokenEndpoint(int statusCode)
    {
        var client = new FakeApiClient();
        client.Enqueue("", statusCode: statusCode);

        var manager = new OAuth2TokenManager();
        manager.SetApiClient(client);

        var ex = await Assert.ThrowsAsync<OAuth2ServerError>(() =>
            manager.GetAccessTokenAsync(
                new Uri("https://auth.example.com/token"),
                new Dictionary<string, string> { ["grant_type"] = "client_credentials" }
            )
        );
        Assert.Equal(statusCode, ex.StatusCode);
        Assert.Equal("redirect_refused", ex.Code);
    }
}
