// Swagger Petstore - OpenAPI 3.0
// A simplified Pet Store API for integration testing.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Pet } from './pet.js';
import { Expose, Type } from 'class-transformer';

export class PetPassport {
  /** @example null */
  @Expose({ name: 'pet' })
  @Type(() => Pet)
  pet?: Pet;
  /**
   * Base64-encoded primary thumbnail
   */
  @Expose({ name: 'thumbnail' })
  thumbnail?: string;
  /**
   * Base64-encoded scans of each passport page
   * @example null
   */
  @Expose({ name: 'scans' })
  scans?: Array<string>;
  /** @example null */
  @Expose({ name: 'issuedAt' })
  issuedAt?: string;

  constructor(data?: Partial<PetPassport>) {
    Object.assign(this, data);
  }

  /**
   * Value-equality based on all declared fields. Generated so model
   * instances work correctly with Set/Map and in test assertions.
   * Field comparison uses JSON.stringify which is deterministic for
   * plain data classes (insertion-order preserved by all engines for
   * string keys).
   */
  equals(other: unknown): boolean {
    if (this === other) return true;
    if (!(other instanceof PetPassport)) return false;
    if (JSON.stringify(this.pet) !== JSON.stringify(other.pet)) {
      return false;
    }
    if (JSON.stringify(this.thumbnail) !== JSON.stringify(other.thumbnail)) {
      return false;
    }
    if (JSON.stringify(this.scans) !== JSON.stringify(other.scans)) {
      return false;
    }
    if (JSON.stringify(this.issuedAt) !== JSON.stringify(other.issuedAt)) {
      return false;
    }
    return true;
  }
}
