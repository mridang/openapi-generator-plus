// Swagger Petstore - OpenAPI 3.0
// A simplified Pet Store API for integration testing.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Expose } from 'class-transformer';

export class WetFood {
  /** @example null */
  @Expose({ name: 'foodType' })
  foodType: string = 'wet';
  /** @example null */
  @Expose({ name: 'volumeMl' })
  volumeMl!: number;

  constructor(data?: Partial<WetFood>) {
    Object.assign(this, data);
    if (data !== undefined) {
      if (this.volumeMl == null) {
        throw new Error('volumeMl is required');
      }
    }
  }

  /**
   * Value-equality based on all declared fields. Generated so model
   * instances work correctly with Set/Map and in test assertions.
   * Field comparison uses JSON.stringify which is deterministic for
   * plain data classes (insertion-order preserved by all engines for
   * string keys).
   */
  equals(other: unknown): boolean {
    if (this === other) return true;
    if (!(other instanceof WetFood)) return false;
    if (JSON.stringify(this.foodType) !== JSON.stringify(other.foodType)) {
      return false;
    }
    if (JSON.stringify(this.volumeMl) !== JSON.stringify(other.volumeMl)) {
      return false;
    }
    return true;
  }
}
