// Swagger Petstore - OpenAPI 3.0
// A simplified Pet Store API for integration testing.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Expose } from "class-transformer";

export class Surgery {
  /** @example null */
  @Expose({ name: "procedureName" })
  procedureName!: string;
  /** @example null */
  @Expose({ name: "durationMinutes" })
  durationMinutes?: number;

  constructor(data?: Partial<Surgery>) {
    Object.assign(this, data);
    if (data !== undefined) {
      if (this.procedureName == null) {
        throw new Error("procedureName is required");
      }
    }
    if (this.procedureName != null && typeof this.procedureName !== "string") {
      throw new TypeError(
        `procedureName must be a string, got ${typeof this.procedureName}`,
      );
    }
    if (
      this.durationMinutes != null &&
      typeof this.durationMinutes !== "number"
    ) {
      throw new TypeError(
        `durationMinutes must be a number, got ${typeof this.durationMinutes}`,
      );
    }
    if (
      this.durationMinutes != null &&
      typeof this.durationMinutes !== "number"
    ) {
      throw new TypeError(
        `durationMinutes must be a number, got ${typeof this.durationMinutes}`,
      );
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof Surgery)) {
      return false;
    }
    if (
      !Surgery.__deepEquals(
        this.procedureName,
        (other as Surgery).procedureName,
      )
    ) {
      return false;
    }
    if (
      !Surgery.__deepEquals(
        this.durationMinutes,
        (other as Surgery).durationMinutes,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => Surgery.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) => bvals.some((w) => Surgery.__deepEquals(v, w)));
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !Surgery.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          Surgery.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
