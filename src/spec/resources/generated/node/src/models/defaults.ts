// Swagger Petstore - OpenAPI 3.0
// A simplified Pet Store API for integration testing.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Expose } from "class-transformer";

export class Defaults {
  /** @example null */
  @Expose({ name: "retries" })
  retries?: number = 3;
  /** @example null */
  @Expose({ name: "mode" })
  mode?: DefaultsModeEnum = DefaultsModeEnum.Medium;
  /** @example null */
  @Expose({ name: "label" })
  label?: string = "untitled";

  constructor(data?: Partial<Defaults>) {
    Object.assign(this, data);
    if (this.retries === undefined) {
      this.retries = 3;
    }
    if (this.mode === undefined) {
      this.mode = DefaultsModeEnum.Medium;
    }
    if (this.label === undefined) {
      this.label = "untitled";
    }
    if (this.retries != null && typeof this.retries !== "number") {
      throw new TypeError(
        `retries must be a number, got ${typeof this.retries}`,
      );
    }
    if (this.label != null && typeof this.label !== "string") {
      throw new TypeError(`label must be a string, got ${typeof this.label}`);
    }
    if (this.mode != null) {
      const modeValues = Object.values(DefaultsModeEnum);
      if (!(modeValues as readonly unknown[]).includes(this.mode)) {
        throw new Error(
          `Unknown enum value for mode: ${JSON.stringify(this.mode)}. ` +
            `Expected one of [${modeValues.map((v) => JSON.stringify(v)).join(", ")}].`,
        );
      }
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof Defaults)) {
      return false;
    }
    if (!Defaults.__deepEquals(this.retries, (other as Defaults).retries)) {
      return false;
    }
    if (!Defaults.__deepEquals(this.mode, (other as Defaults).mode)) {
      return false;
    }
    if (!Defaults.__deepEquals(this.label, (other as Defaults).label)) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => Defaults.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => Defaults.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !Defaults.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          Defaults.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}

export enum DefaultsModeEnum {
  Low = "low",
  Medium = "medium",
  High = "high",
}
