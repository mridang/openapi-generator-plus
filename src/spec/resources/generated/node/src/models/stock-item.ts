// Swagger Petstore - OpenAPI 3.0
// A simplified Pet Store API for integration testing.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Availability } from "./availability.js";
import { Priority } from "./priority.js";
import { Expose } from "class-transformer";

export class StockItem {
  /**
   * 2.5 — Wire-serialization registry for `type: number` (no format)
   * fields. Such fields are carried as branded {@link Decimal} values,
   * which are plain strings at runtime to preserve arbitrary precision.
   * On the request path ObjectSerializer.serialize walks the instance with
   * JSON.stringify, which would quote a string and emit
   * `"weightKg":"12.345"` (a JSON string) instead of the spec-required
   * `"weightKg":12.345` (a JSON number). The serializer consults this set
   * (by runtime property name) to emit those fields unquoted via JSON.rawJSON,
   * preserving the full decimal text without a lossy Number() round-trip.
   * Empty when the model has no `type: number` no-format fields.
   */
  static readonly __decimalFields: ReadonlySet<string> = new Set([]);

  @Expose({ name: "priority" })
  priority!: Priority;
  @Expose({ name: "availability" })
  availability?: Availability;
  /**
   * Quantity grid as an array of integer arrays
   */
  @Expose({ name: "matrix" })
  matrix?: Array<Array<number>>;

  constructor(data?: Partial<StockItem>) {
    Object.assign(this, data);
    if (data !== undefined) {
      if (this.priority == null) {
        throw new Error("priority is required");
      }
    }
    if (
      this.matrix != null &&
      !Array.isArray(this.matrix) &&
      !((this.matrix as unknown) instanceof Set)
    ) {
      throw new TypeError(`matrix must be an array, got ${typeof this.matrix}`);
    }
    if (this.priority != null) {
      const priorityValues = Object.values(Priority).filter(
        (v) =>
          typeof (Priority as Record<string, unknown>)[v as string] !==
          "number",
      );
      if (!(priorityValues as readonly unknown[]).includes(this.priority)) {
        throw new Error(
          `Unknown enum value for priority: ${JSON.stringify(this.priority)}. ` +
            `Expected one of [${priorityValues.map((v) => JSON.stringify(v)).join(", ")}].`,
        );
      }
    }
    if (this.availability != null) {
      const availabilityValues = Object.values(Availability).filter(
        (v) =>
          typeof (Availability as Record<string, unknown>)[v as string] !==
          "number",
      );
      if (
        !(availabilityValues as readonly unknown[]).includes(this.availability)
      ) {
        throw new Error(
          `Unknown enum value for availability: ${JSON.stringify(this.availability)}. ` +
            `Expected one of [${availabilityValues.map((v) => JSON.stringify(v)).join(", ")}].`,
        );
      }
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof StockItem)) {
      return false;
    }
    if (!StockItem.__deepEquals(this.priority, (other as StockItem).priority)) {
      return false;
    }
    if (
      !StockItem.__deepEquals(
        this.availability,
        (other as StockItem).availability,
      )
    ) {
      return false;
    }
    if (!StockItem.__deepEquals(this.matrix, (other as StockItem).matrix)) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => StockItem.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => StockItem.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !StockItem.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          StockItem.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
