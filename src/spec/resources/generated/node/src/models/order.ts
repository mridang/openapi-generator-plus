// Swagger Petstore - OpenAPI 3.0
// A simplified Pet Store API for integration testing.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Expose, Type } from "class-transformer";

export class Order {
  /** @example 10 */
  @Expose({ name: "id" })
  id?: number;
  /** @example 198772 */
  @Expose({ name: "petId" })
  petId?: number;
  /** @example 7 */
  @Expose({ name: "quantity" })
  quantity?: number;
  /** @example null */
  @Expose({ name: "shipDate" })
  @Type(() => Date)
  shipDate?: Date;
  /**
   * Order Status
   * @example approved
   */
  @Expose({ name: "status" })
  status?: OrderStatusEnum = OrderStatusEnum.Placed;
  /** @example null */
  @Expose({ name: "complete" })
  complete?: boolean;

  constructor(data?: Partial<Order>) {
    Object.assign(this, data);
    if (this.status === undefined) {
      this.status = OrderStatusEnum.Placed;
    }
    if (this.id != null && typeof this.id !== "number") {
      throw new TypeError(`id must be a number, got ${typeof this.id}`);
    }
    if (this.petId != null && typeof this.petId !== "number") {
      throw new TypeError(`petId must be a number, got ${typeof this.petId}`);
    }
    if (this.quantity != null && typeof this.quantity !== "number") {
      throw new TypeError(
        `quantity must be a number, got ${typeof this.quantity}`,
      );
    }
    if (this.quantity != null && typeof this.quantity !== "number") {
      throw new TypeError(
        `quantity must be a number, got ${typeof this.quantity}`,
      );
    }
    if (this.complete != null && typeof this.complete !== "boolean") {
      throw new TypeError(
        `complete must be a boolean, got ${typeof this.complete}`,
      );
    }
    if (this.status != null) {
      const statusValues = Object.values(OrderStatusEnum);
      if (!(statusValues as readonly unknown[]).includes(this.status)) {
        throw new Error(
          `Unknown enum value for status: ${JSON.stringify(this.status)}. ` +
            `Expected one of [${statusValues.map((v) => JSON.stringify(v)).join(", ")}].`,
        );
      }
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof Order)) {
      return false;
    }
    if (!Order.__deepEquals(this.id, (other as Order).id)) {
      return false;
    }
    if (!Order.__deepEquals(this.petId, (other as Order).petId)) {
      return false;
    }
    if (!Order.__deepEquals(this.quantity, (other as Order).quantity)) {
      return false;
    }
    if (!Order.__deepEquals(this.shipDate, (other as Order).shipDate)) {
      return false;
    }
    if (!Order.__deepEquals(this.status, (other as Order).status)) {
      return false;
    }
    if (!Order.__deepEquals(this.complete, (other as Order).complete)) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => Order.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) => bvals.some((w) => Order.__deepEquals(v, w)));
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !Order.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          Order.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}

export enum OrderStatusEnum {
  Placed = "placed",
  Approved = "approved",
  Delivered = "delivered",
}
