// Swagger Petstore - OpenAPI 3.0
// A simplified Pet Store API for integration testing.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Color } from "./color.js";
import { Expose, Type, Transform } from "class-transformer";
import { Temporal } from "temporal-polyfill";
import {
  durationFromProtoJson,
  durationToProtoJson,
} from "../object-serializer.js";

/**
 * Edge-case property shapes mirrored from real protobuf-derived specs.
 */
export class EdgeCases {
  /**
   * 2.5 — Wire-serialization registry for `type: number` (no format)
   * fields. Such fields are carried as branded {@link Decimal} values,
   * which are plain strings at runtime to preserve arbitrary precision.
   * On the request path ObjectSerializer.serialize walks the instance with
   * JSON.stringify, which would quote a string and emit
   * `"weightKg":"12.345"` (a JSON string) instead of the spec-required
   * `"weightKg":12.345` (a JSON number). The serializer consults this set
   * (by runtime property name) to emit those fields unquoted via JSON.rawJSON,
   * preserving the full decimal text without a lossy Number() round-trip.
   * Empty when the model has no `type: number` no-format fields.
   */
  static readonly __decimalFields: ReadonlySet<string> = new Set([]);

  @Expose({ name: "freeFormAny" })
  freeFormAny?: unknown;
  @Expose({ name: "favoriteColor" })
  favoriteColor?: Color;
  /**
   * Required permission is "user.grant.write" for <admin> & owner roles (don't skip it)
   */
  @Expose({ name: "permissionsNote" })
  permissionsNote?: string;
  @Expose({ name: "and" })
  and?: boolean;
  @Expose({ name: "or" })
  or?: boolean;
  @Expose({ name: "not" })
  not?: boolean;
  @Expose({ name: "class" })
  class?: string;
  @Expose({ name: "return" })
  return?: string;
  /**
   * Back-off interval before retrying (protobuf-JSON duration, e.g. "3600s")
   */
  @Expose({ name: "retryAfter" })
  /*
   * 4.8 — `format: duration` is a google.protobuf.Duration. The native
   * type stays Temporal.Duration, but the wire form is protobuf-JSON
   * ("3600s"), not ISO-8601 ("PT1H") — Zitadel rejects the ISO form.
   */
  @Transform(
    ({ value }) =>
      typeof value === "string" ? durationFromProtoJson(value) : value,
    { toClassOnly: true },
  )
  @Transform(
    ({ value }) =>
      value instanceof Temporal.Duration ? durationToProtoJson(value) : value,
    { toPlainOnly: true },
  )
  retryAfter?: Temporal.Duration;
  /**
   * Absolute expiry instant
   */
  @Expose({ name: "expiresAt" })
  @Type(() => Date)
  expiresAt?: Date;

  constructor(data?: Partial<EdgeCases>) {
    Object.assign(this, data);
    if (
      this.permissionsNote != null &&
      typeof this.permissionsNote !== "string"
    ) {
      throw new TypeError(
        `permissionsNote must be a string, got ${typeof this.permissionsNote}`,
      );
    }
    if (this.and != null && typeof this.and !== "boolean") {
      throw new TypeError(`and must be a boolean, got ${typeof this.and}`);
    }
    if (this.or != null && typeof this.or !== "boolean") {
      throw new TypeError(`or must be a boolean, got ${typeof this.or}`);
    }
    if (this.not != null && typeof this.not !== "boolean") {
      throw new TypeError(`not must be a boolean, got ${typeof this.not}`);
    }
    if (this.class != null && typeof this.class !== "string") {
      throw new TypeError(`class must be a string, got ${typeof this.class}`);
    }
    if (this.return != null && typeof this.return !== "string") {
      throw new TypeError(`return must be a string, got ${typeof this.return}`);
    }
    /**
     * 4.8 — `format: duration` (google.protobuf.Duration). Accept a
     * protobuf-JSON string ("3600s", parsed via durationFromProtoJson,
     * which throws on bad input) or an already-constructed Duration
     * instance.
     */
    if (this.retryAfter != null) {
      if (typeof this.retryAfter === "string") {
        this.retryAfter = durationFromProtoJson(
          this.retryAfter as unknown as string,
        ) as unknown as Temporal.Duration;
      } else if (!(this.retryAfter instanceof Temporal.Duration)) {
        throw new TypeError(
          `retryAfter must be a Temporal.Duration or protobuf-JSON duration string, got ${typeof this.retryAfter}`,
        );
      }
    }
    if (this.favoriteColor != null) {
      const favoriteColorValues = Object.values(Color).filter(
        (v) =>
          typeof (Color as Record<string, unknown>)[v as string] !== "number",
      );
      if (
        !(favoriteColorValues as readonly unknown[]).includes(
          this.favoriteColor,
        )
      ) {
        throw new Error(
          `Unknown enum value for favoriteColor: ${JSON.stringify(this.favoriteColor)}. ` +
            `Expected one of [${favoriteColorValues.map((v) => JSON.stringify(v)).join(", ")}].`,
        );
      }
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof EdgeCases)) {
      return false;
    }
    if (
      !EdgeCases.__deepEquals(
        this.freeFormAny,
        (other as EdgeCases).freeFormAny,
      )
    ) {
      return false;
    }
    if (
      !EdgeCases.__deepEquals(
        this.favoriteColor,
        (other as EdgeCases).favoriteColor,
      )
    ) {
      return false;
    }
    if (
      !EdgeCases.__deepEquals(
        this.permissionsNote,
        (other as EdgeCases).permissionsNote,
      )
    ) {
      return false;
    }
    if (!EdgeCases.__deepEquals(this.and, (other as EdgeCases).and)) {
      return false;
    }
    if (!EdgeCases.__deepEquals(this.or, (other as EdgeCases).or)) {
      return false;
    }
    if (!EdgeCases.__deepEquals(this.not, (other as EdgeCases).not)) {
      return false;
    }
    if (!EdgeCases.__deepEquals(this.class, (other as EdgeCases).class)) {
      return false;
    }
    if (!EdgeCases.__deepEquals(this.return, (other as EdgeCases).return)) {
      return false;
    }
    if (
      !EdgeCases.__deepEquals(this.retryAfter, (other as EdgeCases).retryAfter)
    ) {
      return false;
    }
    if (
      !EdgeCases.__deepEquals(this.expiresAt, (other as EdgeCases).expiresAt)
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => EdgeCases.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => EdgeCases.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !EdgeCases.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          EdgeCases.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
