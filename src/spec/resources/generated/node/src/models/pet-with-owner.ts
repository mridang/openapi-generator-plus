// Swagger Petstore - OpenAPI 3.0
// A simplified Pet Store API for integration testing.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Category } from "./category.js";
import { Tag } from "./tag.js";
import { Expose, Type } from "class-transformer";
import { URI } from "../brand.js";
import { Email } from "../brand.js";
import { Decimal } from "../brand.js";

/**
 * A pet record extended with owner information
 */
export class PetWithOwner {
  /** @example 10 */
  @Expose({ name: "id" })
  id?: number;
  /** @example doggie */
  @Expose({ name: "name" })
  name!: string;
  /** @example null */
  @Expose({ name: "category" })
  @Type(() => Category)
  category?: Category;
  /** @example null */
  @Expose({ name: "photoUrls" })
  photoUrls!: Set<string>;
  /** @example null */
  @Expose({ name: "tags" })
  @Type(() => Tag)
  tags?: Array<Tag>;
  /**
   * pet status in the store
   * @example null
   * @deprecated This property is deprecated.
   */
  @Expose({ name: "status" })
  status?: PetWithOwnerStatusEnum;
  /** @example null */
  @Expose({ name: "location" })
  location?: Array<unknown>;
  /**
   * Absolute URL to the pet's public profile page
   * @example https://example.com/pets/fido
   */
  @Expose({ name: "homepageUrl" })
  homepageUrl?: URI;
  /**
   * Optionally-relative thumbnail location
   * @example /assets/thumb-fido.png
   */
  @Expose({ name: "thumbnailRef" })
  thumbnailRef?: string;
  /**
   * RFC 6570 template for related-resource links
   * @example https://example.com/pets/{id}/photos{?size}
   */
  @Expose({ name: "linkTemplate" })
  linkTemplate?: string;
  /** @example null */
  @Expose({ name: "ownerEmail" })
  ownerEmail?: Email;
  /**
   * Pet weight in kilograms (decimal precision)
   * @example 12.345
   */
  @Expose({ name: "weightKg" })
  weightKg?: Decimal;
  /** @example null */
  @Expose({ name: "ownerName" })
  ownerName!: string;

  constructor(data?: Partial<PetWithOwner>) {
    Object.assign(this, data);
    if (data !== undefined) {
      if (this.name == null) {
        throw new Error("name is required");
      }
      if (this.photoUrls == null) {
        throw new Error("photoUrls is required");
      }
      if (this.ownerName == null) {
        throw new Error("ownerName is required");
      }
    }
    if (this.id != null && typeof this.id !== "number") {
      throw new TypeError(`id must be a number, got ${typeof this.id}`);
    }
    if (this.name != null && typeof this.name !== "string") {
      throw new TypeError(`name must be a string, got ${typeof this.name}`);
    }
    if (
      this.photoUrls != null &&
      !Array.isArray(this.photoUrls) &&
      !((this.photoUrls as unknown) instanceof Set)
    ) {
      throw new TypeError(
        `photoUrls must be an array, got ${typeof this.photoUrls}`,
      );
    }
    if (
      this.location != null &&
      !Array.isArray(this.location) &&
      !((this.location as unknown) instanceof Set)
    ) {
      throw new TypeError(
        `location must be an array, got ${typeof this.location}`,
      );
    }
    if (this.homepageUrl != null && typeof this.homepageUrl !== "string") {
      throw new TypeError(
        `homepageUrl must be a string, got ${typeof this.homepageUrl}`,
      );
    }
    if (this.thumbnailRef != null && typeof this.thumbnailRef !== "string") {
      throw new TypeError(
        `thumbnailRef must be a string, got ${typeof this.thumbnailRef}`,
      );
    }
    if (this.linkTemplate != null && typeof this.linkTemplate !== "string") {
      throw new TypeError(
        `linkTemplate must be a string, got ${typeof this.linkTemplate}`,
      );
    }
    if (this.ownerEmail != null && typeof this.ownerEmail !== "string") {
      throw new TypeError(
        `ownerEmail must be a string, got ${typeof this.ownerEmail}`,
      );
    }
    /**
     * 2.5 — `type: number` (no numeric format). Carried as a branded
     * Decimal string to preserve arbitrary precision. The wire form can
     * arrive as a JSON number (no explicit format means no precision
     * guarantee from the server); coerce to its canonical string form
     * here so downstream typed-string ops are safe.
     */
    if (this.weightKg != null && typeof this.weightKg === "number") {
      this.weightKg = String(this.weightKg) as unknown as Decimal;
    } else if (this.weightKg != null && typeof this.weightKg !== "string") {
      throw new TypeError(
        `weightKg must be a Decimal string or number, got ${typeof this.weightKg}`,
      );
    }
    if (this.ownerName != null && typeof this.ownerName !== "string") {
      throw new TypeError(
        `ownerName must be a string, got ${typeof this.ownerName}`,
      );
    }
    if (this.status != null) {
      const statusValues = Object.values(PetWithOwnerStatusEnum);
      if (!(statusValues as readonly unknown[]).includes(this.status)) {
        throw new Error(
          `Unknown enum value for status: ${JSON.stringify(this.status)}. ` +
            `Expected one of [${statusValues.map((v) => JSON.stringify(v)).join(", ")}].`,
        );
      }
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof PetWithOwner)) {
      return false;
    }
    if (!PetWithOwner.__deepEquals(this.id, (other as PetWithOwner).id)) {
      return false;
    }
    if (!PetWithOwner.__deepEquals(this.name, (other as PetWithOwner).name)) {
      return false;
    }
    if (
      !PetWithOwner.__deepEquals(
        this.category,
        (other as PetWithOwner).category,
      )
    ) {
      return false;
    }
    if (
      !PetWithOwner.__deepEquals(
        this.photoUrls,
        (other as PetWithOwner).photoUrls,
      )
    ) {
      return false;
    }
    if (!PetWithOwner.__deepEquals(this.tags, (other as PetWithOwner).tags)) {
      return false;
    }
    if (
      !PetWithOwner.__deepEquals(this.status, (other as PetWithOwner).status)
    ) {
      return false;
    }
    if (
      !PetWithOwner.__deepEquals(
        this.location,
        (other as PetWithOwner).location,
      )
    ) {
      return false;
    }
    if (
      !PetWithOwner.__deepEquals(
        this.homepageUrl,
        (other as PetWithOwner).homepageUrl,
      )
    ) {
      return false;
    }
    if (
      !PetWithOwner.__deepEquals(
        this.thumbnailRef,
        (other as PetWithOwner).thumbnailRef,
      )
    ) {
      return false;
    }
    if (
      !PetWithOwner.__deepEquals(
        this.linkTemplate,
        (other as PetWithOwner).linkTemplate,
      )
    ) {
      return false;
    }
    if (
      !PetWithOwner.__deepEquals(
        this.ownerEmail,
        (other as PetWithOwner).ownerEmail,
      )
    ) {
      return false;
    }
    if (
      !PetWithOwner.__deepEquals(
        this.weightKg,
        (other as PetWithOwner).weightKg,
      )
    ) {
      return false;
    }
    if (
      !PetWithOwner.__deepEquals(
        this.ownerName,
        (other as PetWithOwner).ownerName,
      )
    ) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => PetWithOwner.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) =>
        bvals.some((w) => PetWithOwner.__deepEquals(v, w)),
      );
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !PetWithOwner.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          PetWithOwner.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}

export enum PetWithOwnerStatusEnum {
  Available = "available",
  Pending = "pending",
  Sold = "sold",
}
