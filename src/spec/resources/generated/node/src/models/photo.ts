// Swagger Petstore - OpenAPI 3.0
// A simplified Pet Store API for integration testing.
//
// The version of the OpenAPI document: 1.0.0
//
// Generated by: https://openapi-generator.tech

import { Expose } from "class-transformer";

export class Photo {
  /** @example null */
  @Expose({ name: "id" })
  id?: number;
  /** @example null */
  @Expose({ name: "caption" })
  caption?: string;
  /** @example null */
  @Expose({ name: "isPrimary" })
  isPrimary?: boolean;
  /** @example null */
  @Expose({ name: "url" })
  url?: string;

  constructor(data?: Partial<Photo>) {
    Object.assign(this, data);
    if (this.id != null && typeof this.id !== "number") {
      throw new TypeError(`id must be a number, got ${typeof this.id}`);
    }
    if (this.caption != null && typeof this.caption !== "string") {
      throw new TypeError(
        `caption must be a string, got ${typeof this.caption}`,
      );
    }
    if (this.isPrimary != null && typeof this.isPrimary !== "boolean") {
      throw new TypeError(
        `isPrimary must be a boolean, got ${typeof this.isPrimary}`,
      );
    }
    if (this.url != null && typeof this.url !== "string") {
      throw new TypeError(`url must be a string, got ${typeof this.url}`);
    }
  }

  /**
   * Value-equality across all declared fields. Two instances are equal when
   * every field deep-equals the other's. Arrays, plain objects, Sets, Maps,
   * Buffers and Date values are compared structurally; nested model fields
   * fall through to their own {@link equals} when present, otherwise to a
   * recursive structural compare. Mirrors the value-equality exposed by the
   * other SDKs (node-model-equality).
   */
  equals(other: unknown): boolean {
    if (this === other) {
      return true;
    }
    if (!(other instanceof Photo)) {
      return false;
    }
    if (!Photo.__deepEquals(this.id, (other as Photo).id)) {
      return false;
    }
    if (!Photo.__deepEquals(this.caption, (other as Photo).caption)) {
      return false;
    }
    if (!Photo.__deepEquals(this.isPrimary, (other as Photo).isPrimary)) {
      return false;
    }
    if (!Photo.__deepEquals(this.url, (other as Photo).url)) {
      return false;
    }
    return true;
  }

  /**
   * Recursive structural comparison used by {@link equals}. Defers to a
   * nested value's own `equals` method when it exposes one (generated
   * models do), so equality stays value-based all the way down.
   */
  private static __deepEquals(a: unknown, b: unknown): boolean {
    if (a === b) {
      return true;
    }
    if (a == null || b == null) {
      return a === b;
    }
    if (
      typeof (a as { equals?: unknown }).equals === "function" &&
      a.constructor === b.constructor
    ) {
      return (a as { equals(o: unknown): boolean }).equals(b);
    }
    if (Buffer.isBuffer(a) && Buffer.isBuffer(b)) {
      return a.equals(b);
    }
    if (a instanceof Date && b instanceof Date) {
      return a.getTime() === b.getTime();
    }
    if (Array.isArray(a) && Array.isArray(b)) {
      if (a.length !== b.length) {
        return false;
      }
      return a.every((v, i) => Photo.__deepEquals(v, b[i]));
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const bvals = [...b];
      return [...a].every((v) => bvals.some((w) => Photo.__deepEquals(v, w)));
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      for (const [k, v] of a) {
        if (!b.has(k) || !Photo.__deepEquals(v, b.get(k))) {
          return false;
        }
      }
      return true;
    }
    if (typeof a === "object" && typeof b === "object") {
      const ar = a as Record<string, unknown>;
      const br = b as Record<string, unknown>;
      const ak = Object.keys(ar);
      const bk = Object.keys(br);
      if (ak.length !== bk.length) {
        return false;
      }
      return ak.every(
        (k) =>
          Object.prototype.hasOwnProperty.call(br, k) &&
          Photo.__deepEquals(ar[k], br[k]),
      );
    }
    return false;
  }
}
