.PHONY: install build test lint format typecheck clean

install:
	npm install

build: install
	npx tsc

test: install
	npx jest --verbose

lint: install
	npx eslint .

format: install
	npx prettier --write .

typecheck: install
	npx tsc --noEmit

clean:
	rm -rf node_modules dist coverage
